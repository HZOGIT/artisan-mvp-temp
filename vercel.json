{
  "buildCommand": "pnpm install --frozen-lockfile && cd client && pnpm build",
  "outputDirectory": "client/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "env": {
    "VITE_API_URL": "@vite_api_url",
    "VITE_APP_TITLE": "@vite_app_title"
  }
}
