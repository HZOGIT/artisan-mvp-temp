receivers:
  - name: ntfy
    webhook_configs:
      - url: https://ntfy.sh/${TOPIC}
        send_resolved: true

route:
  receiver: ntfy
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
