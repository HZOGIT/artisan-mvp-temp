# Journal — Refonte clean-archi (post-migration PG)

> Source de vérité du **cron itératif** de la refonte. À lire au début de CHAQUE itération
> pour récupérer la **Prochaine action**, puis l'exécuter, gater, tracer (4 canaux), et
> mettre à jour ce fichier. Survit à la compaction de contexte.

## Directive de persistance (IMPORTANT)
**Ne PAS s'arrêter tant que la refonte n'est pas parachevée** (Phase 0 → phases 1-5 → extinction de l'ancien stack). Le cron itératif enchaîne les sous-tâches d'itération en itération jusqu'au bout — ne pas arrêter la boucle sur un simple jalon atteint, ne pas demander de confirmation pour avancer. **Seule exception** : problème d'intégrité financière/sécurité **irréparable** révélé par un test → STOP + ntfy ALERT. Ne supprimer le cron qu'à complétion réelle (ou demande explicite de l'utilisateur).

## Décisions verrouillées
- **Framework serveur cible = Fastify** (choisi 2026-06-13, vs Hono). Le nouveau stack se scaffolde en Fastify (`src/app.ts` + adapter tRPC + `/health`), adapté progressivement derrière le gateway/flag. tRPC conservé, Drizzle (node-postgres).

## Convention de code (IMPORTANT)
- **Pas de références ticket `OPE-XXX` dans les commentaires de code** — seulement dans les messages de commit (et ce journal/docs). Cleanup du legacy = carte OPE-255 (backlog).
- Code neuf clean-archi dans **`src/**`** (gate : `pnpm exec tsc -p tsconfig.src.json`). Le legacy (`server/`, `client/`) garde sa dette tsc (hors scope).

## Plan de référence
- `docs/architecture/ope-184-plan-migration-detaille.md` (recette par domaine + tableau Phase 0 + phases 1-5).
- `docs/architecture/ope-184-proposition-stack-cible.md` (stack cible : Fastify + tRPC + Drizzle pg + clean-archi + RLS).
- Projet Linear : « Refonte progressive de la stack et de l'architecture ».

## Recette par domaine (checklist de chaque epic phases 1→5)
1. Cartographier le domaine (entités, invariants, effets de bord). 2. Définir le **port** `I<Domaine>Repository` (chaque méthode exige le `TenantContext`). 3. Repo **Drizzle** (filtre artisanId + `withTenant()` RLS). 4. **Use-cases** (logique métier pure, ports injectés). 5. Tests unit (use-cases mockés) + intégration (repo sur PG jetable) + isolation cross-tenant. 6. Adapter tRPC (`<domaine>.*`). 7. Câbler le **gateway** derrière un flag (off par défaut). 8. Parité/canary → bascule du flag → on.

## Gate par itération
- `pnpm exec tsc -p tsconfig.src.json` vert (code neuf).
- Tests du code neuf verts (Vitest, PG jetable quand applicable).
- Pour les domaines sensibles : préserver les invariants métier (facturation, TVA, FEC débit=crédit, isolation cross-tenant, anti self-approbation, etc.).

## Règle de traçabilité (4 canaux, par itération réussie)
1. **git** : commit + push `staging` (JAMAIS CLAUDE.md ni terraform/*). 2. **journal** : MAJ ce fichier (Prochaine action). 3. **ntfy** : `devtools/agents/ntfy-pub.sh "titre" "msg" "tag"` (channel operioz-claude-code-2026). 4. **Linear** : `save_comment` sur l'issue active du sous-batch (ou l'issue Phase 0 concernée). STOP + ntfy ALERT seulement sur un problème d'intégrité (financier/sécurité) irréparable.

---

## État de départ (2026-06-13)
- **Migration MySQL→PG TERMINÉE** (OPE-193 Done) : code 100% Drizzle dialect-aware, schéma PG 103 tables, data staging copiée+intègre, app staging **live sur PostgreSQL**, mysql arrêté. C'est le socle data de la refonte.
- **Phase 0 issues 0.1–0.9 = FAITES** (toute la bascule DB).
- **`src/**` n'existe pas encore** (aucun code clean-archi). `tsconfig.src.json` prêt (include `src/**` + schema.pg.ts).

## Découpage de la suite (cibles)
- **Phase 0 kernel/socle (0.10–0.20 + QW)** : TenantContext (0.10), withTenant() RLS (0.11), migration RLS policies (0.12), ports effets de bord Email/Sms/Storage/Pdf (0.13), CI lint+typecheck (0.14), CI vitest+PG (0.15), sortir migrations du boot (0.16), gateway flag (0.17), scaffold Fastify src/app.ts + /health (0.18), feature flags (0.19), harnais isolation cross-tenant (0.20), quick-win deps mortes (QW).
- **Phases 1→5** (epics par domaine, recette ci-dessus) : 1=pilotes (vehicules, badges, support, avis, geoloc), 2=référentiels (clients, articles, fournisseurs, stocks, parametres), 3=cœur transactionnel (devis, factures, contrats, comptabilité, commandes-fourn), 4=terrain/temps réel (interventions, chantiers, rdv, notifs-push, assistant-ia SSE), 5=plateforme/bascule (stripe, users-permissions, auth+révocation, scheduler, extinction ancien stack).

## Issues Linear Phase 0 (existent déjà)
0.10=OPE-196, 0.11=OPE-197 (withTenant), 0.12=OPE-198 (RLS, Urgent), 0.20=OPE-206 (harnais isolation, Urgent). Phase 1 vehicules gabarit = OPE-208→216.

## Avancement
- **R0.10 (OPE-196) FAIT** (2026-06-13) : shared kernel `src/shared/tenant/`. `TenantContext` (artisanId, userId, role?) + `TokenClaims` (userId, email) + port `TenantResolver` (resolve(claims)→TenantContext, l'adapter Drizzle DB viendra avec withTenant/repos) + erreurs `UnauthenticatedError`/`MissingTenantError`. `verifyAuthToken(token, secret)` jose HS256 **secret injecté** (pur, testable, **découplé du legacy** — pas d'import auth-simple) + `extractTokenFromCookieHeader`. **Choix clean-archi** : le kernel ne dépend PAS du legacy ; la résolution `artisanId`/permissions depuis la DB est un **port** (adapter implémenté plus tard côté infra). Gate : `tsc -p tsconfig.src.json` vert + **12/12 tests** (`src/shared/tenant/jwt.test.ts`). vitest.config étendu à `src/**/*.test.ts`.

- **R0.11 (OPE-197) FAIT** (2026-06-13) : `src/shared/db/`. `client.ts` : `createDbClient(connStr)` (drizzle node-postgres + pg Pool, injectable) + `getDbHandle()` (défaut lazy via DATABASE_URL). `with-tenant.ts` : `withTenant(db, ctx, fn)` ouvre une transaction Drizzle, `set_config('app.tenant', <artisanId>, true)` (valeur en **paramètre lié**, anti-injection), exécute `fn(tx)` ; + `dbForTenant(ctx, fn)` (client défaut) que les repos utiliseront. **Validé PG** (`with-tenant.test.ts`, 4/4) : `app.tenant` positionné dans la tx (=artisanId), **local à la tx** (revient à vide après, pas de fuite), 2 tenants successifs isolés (11/22), propagation du résultat. tsc src vert.
  - ⚠️ **Note pour R0.12 (RLS)** : `current_setting('app.tenant', true)` revient à **`''` (chaîne vide)** hors transaction (pas null). Les policies RLS doivent utiliser **`nullif(current_setting('app.tenant', true), '')::int`** (sinon `''::int` plante). Vide → 0 ligne (deny).

- **R0.12a (OPE-198, en cours) FAIT** (2026-06-13) : **mécanisme RLS prouvé + SQL généré pour les 64 tables tenant**. ⚠️ **Découverte bloquante** : le rôle `artisan_user` est **SUPERUSER + BYPASSRLS** → il ignore totalement RLS. Conséquence : (a) RLS n'a d'effet que via un **rôle applicatif NON-superuser** (à créer pour le nouveau stack) ; (b) **avantage dual-stack** : appliquer RLS aux tables est **safe pour le legacy** (il bypasse en superuser → non impacté), seul le nouveau stack (rôle dédié) sera contraint.
  - `scripts/rls/generate-tenant-rls.mjs` : interroge information_schema, émet `drizzle/rls/tenant-isolation.sql` — pour chaque table tenant : `ENABLE` + `FORCE ROW LEVEL SECURITY` + policy `tenant_isolation USING/WITH CHECK (<col> = nullif(current_setting('app.tenant', true), '')::int)`. Gère les **2 conventions de colonne** (`artisan_id` snake / `artisanId` camel). Idempotent (DROP POLICY IF EXISTS). 64 tables.
  - **Validé PG** (`src/shared/db/rls.test.ts`, 1/1) via un **rôle non-superuser** de démo : sans tenant → 0 ligne, tenant 1 → ses 2 lignes, tenant 2 → sa 1 ligne, reset → 0 ; le **superuser bypasse** (voit les 3). tsc src vert.
  - **NON appliqué aux vraies tables** (dev/staging) : c'est R0.12b.

- **R0.12b (OPE-198) FAIT** (2026-06-13, RLS = Done) : **rôle app non-superuser + RLS appliquée + nouveau stack câblé**.
  - `scripts/rls/setup-app-role.mjs` : crée/configure le rôle LOGIN **`app_tenant`** (nosuperuser, **nobypassrls**), grants `connect/usage/select,insert,update,delete on all tables` + `usage,select on sequences` + default privileges. Idempotent. (Vérifié : superuser=false, bypassrls=false.)
  - **DENYLIST** ajoutée au générateur : `users`, `active_sessions`, `devices`, `subscriptions` **exclues** de la RLS tenant (lues hors contexte tenant : auth, session, device, webhook Stripe par customerId — sinon le nouveau stack ne pourrait plus authentifier/résoudre le tenant/traiter les webhooks). Le SQL **désactive** explicitement la RLS sur ces 4 tables. → **60 tables** sous RLS, 4 exclues.
  - **Appliqué sur dev PG** : 60 tables `rowsecurity=true`, les 4 exclues `false`. (Legacy dev = superuser → bypasse → non impacté.)
  - **`client.ts`** : `getDbHandle()` préfère **`APP_DATABASE_URL`** (rôle app non-superuser, soumis RLS) puis `DATABASE_URL`. Le legacy garde `DATABASE_URL` (superuser, bypasse).
  - **Validé PG** (`src/shared/db/rls-real-table.test.ts`, + suite `src/shared` 18/18) : sur la vraie table **`clients`** via le rôle app + `withTenant` → tenant A voit ses 2 clients, B voit son 1 ; **hors withTenant → 0** (deny) ; le **superuser voit les 3** (app live non impactée). tsc src vert.
  - ⚠️ **Reste pour le cutover du nouveau stack** (pas maintenant) : (a) créer le rôle `app_tenant` + appliquer `tenant-isolation.sql` **sur staging PG** (deploy, safe car superuser bypasse) ; (b) définir `APP_DATABASE_URL` (rôle app_tenant) dans l'env du nouveau stack. À faire quand le nouveau stack Fastify sera déployé derrière le gateway.

- **R0.20 (OPE-206) FAIT** (2026-06-13) : **harnais d'isolation cross-tenant réutilisable** + erreurs de domaine. `src/shared/errors.ts` : `NotFoundError`/`ForbiddenError`/`ConflictError`/`ValidationError` (avec `code`). `src/shared/testing/cross-tenant.ts` : `expectCrossTenantDenied(action)` — un accès cross-tenant est validé s'il **lève un refus** (NOT_FOUND/FORBIDDEN/Unauthenticated/MissingTenant, par nom ou `code`, TRPCError inclus) **ou renvoie vide** (null/undefined/[]) ; toute fuite (renvoi de la ressource) ou erreur inattendue → échec explicite. + `isCrossTenantDenial`. **Validé** (`src/shared` 28/28) : tests unitaires du harnais (refus throw, retour vide, détection de fuite, erreur inattendue) + **démonstration RLS réelle** (`cross-tenant.rls.test.ts`) : tenant A ne lit pas le client de B (non-leak via le harnais), contrôle que B lit bien le sien. tsc src vert. → filet réutilisable par chaque domaine (phases 1-5).

- **R0.13 (OPE-199) FAIT** (2026-06-13) : **ports d'effets de bord** dans `src/shared/ports/`. Interfaces `EmailPort` (send({to,subject,body})), `SmsPort` (send({to,message})), `StoragePort` (put/get/delete/url), `PdfPort` (render(template,data)→Buffer). **Fakes** in-memory (`FakeEmailPort` avec `sent[]`+`failOnce()`, `FakeSmsPort`, `InMemoryStoragePort`, `FakePdfPort`). **Adapter** `LegacyEmailAdapter` → wrappe `server/_core/emailService.sendEmail` (throw si !success). **Validé** (`src/shared` 33/33, dont ports 5/5) : use-case fictif `envoyerBienvenue(emailPort)` → fake enregistre l'envoi + propage l'échec ; storage put/get/url/delete ; pdf render. tsc src vert.
  - 🔑 **Pattern découplage legacy** : l'adapter importe le legacy via une **variable de chemin (type `string`, non littéral)** → tsc ne tire PAS le graphe legacy (server/db.ts…) dans le gate `tsconfig.src.json`, tout en câblant au runtime. À réutiliser pour tout adapter legacy.

- **R0.18a (OPE-204, en cours) FAIT** (2026-06-13) : **scaffold Fastify**. Dép `fastify@^5.8.5` ajoutée (installée conteneur **et host** — node_modules host séparé, gate `tsc` tourne sur host). `src/app.ts` : `buildApp(): FastifyInstance` expose **`GET /health` → 200 `{status:'ok'}`**. `src/server.ts` : entrée bootable (`listen` sur `NEW_STACK_PORT`/`PORT`/3001). **Validé** (`src/app.test.ts`, 2/2 ; suite src 35/35) : `/health` 200 via `app.inject` (test sans port), route inconnue → 404. tsc src vert.
  - ⚠️ **Note** : toute nouvelle dép doit être installée **côté host aussi** (`pnpm install` host) sinon le gate `tsc` host échoue (node_modules host ≠ volume conteneur).

- **R0.18b (OPE-204) FAIT** (2026-06-13, OPE-204 = Done) : **adapter tRPC sur Fastify + contexte kernel**. Dép `@fastify/cookie@^11` (host+conteneur). `src/interface/trpc/` : `context.ts` (`makeCreateContext({jwtSecret?,resolver?})` → extrait cookie `token`, `verifyAuthToken` → claims, `resolver.resolve(claims)` → tenant ; resolver DB injectable, stub pour l'instant → tenant null), `trpc.ts` (`initTRPC.context<AppContext>()` ; `router`, `publicProcedure`, `protectedProcedure` qui exige `ctx.tenant` non-null → UNAUTHORIZED sinon), `router.ts` (`appRouter` = `{ health }` public ; `AppRouter` exporté). `app.ts` : `@fastify/cookie` + `fastifyTRPCPlugin` monté sur **`/api/trpc`** ; `buildApp(deps)` accepte les `ContextDeps` (jwtSecret/resolver injectables). **Validé** (`src/app.test.ts` 4/4, suite src 37/37) : `/health` 200, `GET /api/trpc/health` → 200 `{result:{data:{status:'ok'}}}`, procédure inconnue → 404. tsc src vert.
  - → Le nouveau stack Fastify+tRPC est **bootable et servi** ; le contexte construit le `TenantContext` via le kernel (R0.10) ; `protectedProcedure` prête (refusera tant que le resolver DB n'est pas branché).

- **R0.17 + R0.19 (logique) FAIT** (2026-06-13, OPE-203/205 cœurs) : **gateway — décision de routage + modèle de feature flags** (pur, testable). `src/interface/gateway/` : `flags.ts` (`DomainFlag {enabled, tenantAllowlist?, tenantDenylist?}`, `FeatureFlags`, `NO_FLAGS`, `parseFlagsFromEnv` : `NEW_STACK_DOMAINS` + `NEW_STACK_CANARY_<DOMAINE>`), `router-decision.ts` : `shouldRouteToNewStack(domain, tenantId, flags)` / alias `isMigrated(domain, artisanId, flags)` (**off par défaut** ; ordre : denylist > enabled global > allowlist canary) + `domainFromTrpcPath("vehicules.list")→"vehicules"`. **Validé** (`gateway.test.ts` 9/9) : off par défaut, enabled global, canary allowlist, denylist (rollback prioritaire), parse env, extraction domaine. tsc src vert.
  - **Reste (infra/déploiement, au moment de la 1ère bascule)** : câblage **proxy Cloudflare** `functions/api/[[path]].js` consommant `shouldRouteToNewStack` (OPE-203), et **source DB** des flags (table `migration_flags`) en plus de l'env (OPE-205). Ces 2 morceaux se feront avec le cutover vehicules (OPE-216). → OPE-203/205 restent In Progress.

- **P1 vehicules 1/9 (OPE-208) FAIT** (2026-06-13) : **scaffold module + port**. `src/modules/vehicules/` : `domain/vehicule.ts` (types **découplés Drizzle** : `Vehicule`, `EntretienVehicule`, `AssuranceVehicule` + enums carburant/statut/entretien/assurance + `Create*Input`/`UpdateVehiculeInput` — **artisanId JAMAIS dans les Input** de création, forcé depuis le TenantContext), `application/vehicule-repository.ts` (**port `IVehiculeRepository`** : list/getById/create/update/delete/updateKilometrage + entretiens (list/add) + assurances (list/add) — **chaque méthode exige `TenantContext`**), `vehicules.module.ts` (wiring DI : `createVehiculesModule({repository})`). **Note** : entretiens/assurances sont scopés via `vehiculeId` → l'impl devra vérifier l'appartenance du véhicule au tenant (ressource hors tenant → null). **Validé** (`vehicules.module.test.ts` 2/2) : stub repo satisfait le port + module câble le repo. tsc src vert.

- **P1 vehicules 2/9 (OPE-209) FAIT** (2026-06-13) : **repository Drizzle scopé tenant**. `infra/vehicule-repository-drizzle.ts` : `VehiculeRepositoryDrizzle implements IVehiculeRepository`, **toutes les requêtes via `withTenant(this.db, ctx, tx⇒…)`** (R0.11, RLS) **+ filtre `artisanId` explicite** (double cloisonnement). Tables importées de `drizzle/schema.pg` (PG-only). Mappers row↔domaine (`toVehicule/toEntretien/toAssurance`). `create` force `artisanId=ctx.artisanId` (jamais depuis l'input). `updateKilometrage` : `GREATEST(kilometrageActuel, new)` (**compteur ne recule pas**). entretiens/assurances : helper `ownsVehicule(tx,ctx,vehiculeId)` vérifié AVANT tout accès (ces tables n'ont pas d'artisanId / hors RLS → l'isolation passe par le véhicule parent). **Validé PG** (`vehicule-repository-drizzle.test.ts`, 4/4 ; suite src 52/52) via le **rôle app non-superuser + RLS** : CRUD scopé, **isolation cross-tenant** (B ne lit/update/delete pas le véhicule de A — via le harnais `expectCrossTenantDenied`), kilométrage non régressif, entretiens/assurances refusés à B. tsc src vert.

- **P1 vehicules 3/9 (OPE-210) FAIT** (2026-06-13) : **use-cases lecture + refus cross-tenant**. `application/read-use-cases.ts` (purs, repo injecté) : `listVehicules(repo, ctx)`, `getVehiculeById(repo, ctx, id)` → null du repo mappé en **`NotFoundError`** (ne révèle pas l'existence). `infra/vehicule-repository-fake.ts` : **`FakeVehiculeRepository`** in-memory complet (implémente tout `IVehiculeRepository`, scoping tenant reproduit) — réutilisable pour les use-cases écriture (4/9). **Validé** (`read-use-cases.test.ts` 4/4, sans DB) : list scopée, getById OK, **cross-tenant → NotFoundError** (assertion directe + harnais `expectCrossTenantDenied`), id inexistant → NotFoundError. tsc src vert.

- **P1 vehicules 4/9 (OPE-211) FAIT** (2026-06-13) : **use-cases écriture + invariants**. `application/write-use-cases.ts` : `createVehicule` (ValidationError si immatriculation vide ; artisanId forcé par le repo), `updateVehicule`/`deleteVehicule`/`enregistrerKilometrage`/`ajouterEntretien`/`ajouterAssurance` (null/false du repo → **NotFoundError**). `enregistrerKilometrage` valide km≥0 (ValidationError) ; invariant non-régressif porté par le repo (GREATEST). **Validé** (`write-use-cases.test.ts` 6/6, sans DB, FakeVehiculeRepository) : create scopé + immatriculation requise, **update/delete cross-tenant → NotFoundError** (harnais ; ressource de A inchangée), kilométrage non régressif (8000 puis 6000→8000) + validation, entretien/assurance refusés hors tenant, delete + double-delete→NotFound. tsc src vert (suite src 61/61). NB : use-case throw synchrone → fonction rendue `async` pour que `.rejects` capture (pattern à retenir).

- **P1 vehicules 5a/9 (OPE-212, en cours) FAIT** (2026-06-13) : **TenantResolver DB → `protectedProcedure` débloquée end-to-end**. `src/shared/tenant/drizzle-tenant-resolver.ts` : `DrizzleTenantResolver implements TenantResolver` — résout depuis `TokenClaims.userId` l'`artisanId` (lecture `artisans` par userId) + `role` (lecture `users`) → `TenantContext` ; null si pas d'artisan (tables hors RLS, lisibles sans tenant). **Pas** ajouté au barrel kernel pur (évite de coupler les consommateurs purs à la DB). `appRouter` : ajout d'une procédure **`whoami`** (`protectedProcedure`, démo) renvoyant le tenant. **Validé PG** (`src/interface/trpc/protected.test.ts`, 3/3 ; suite src 64/64) : cookie token valide → `whoami` renvoie {artisanId résolu, userId, role:'artisan'} (200) ; sans cookie → **401** ; user sans artisan → **401**. tsc src vert.

- **P1 vehicules 5b/9 (OPE-212) FAIT** (2026-06-13, OPE-212 = Done) : **routeur tRPC `vehicules.*` + mapping erreurs + e2e**. `interface/trpc/vehicules.router.ts` : `createVehiculesRouter(repo)` — 10 procédures `protectedProcedure` (list/getById/create/update/delete/updateKilometrage + entretiens/assurances), inputs **Zod** (enums carburant/statut/entretien/assurance), appelant les use-cases avec `ctx.tenant`. **Mapping erreurs domaine→tRPC** dans `trpc.ts` (middleware `mapDomainErrors`) : NotFoundError→NOT_FOUND(404), ValidationError→BAD_REQUEST(400), ForbiddenError→FORBIDDEN(403), ConflictError→CONFLICT(409). 🔑 **Piège tRPC v11** : `next()` ne throw pas → renvoie `{ok:false, error}` ; on mappe via `result.error.cause` (try/catch ne marche pas). `appRouter`→**`createAppRouter(deps)`** (repo injecté), `buildApp(deps)` construit le repo Drizzle (défaut `getDbHandle().db` = rôle app/RLS ; injectable en test). **Validé PG** (`vehicules.router.test.ts` 4/4 + protected 3/3 ; suite src 68/68) : sans cookie→401, create+list tenant A→200, **isolation cross-tenant** (B getById→404, list exclut, update→404), Zod immatriculation vide→400. tsc src vert.

**→ 🎯 Le 1er domaine (vehicules) est servi de bout en bout par le nouveau stack** (HTTP→Fastify→tRPC protectedProcedure→use-case→repo Drizzle→withTenant/RLS), avec isolation tenant prouvée à chaque couche.

- **P1 vehicules — delete cascade (OPE-212, dette) FAIT** (2026-06-13) : **`deleteVehicule` cascade l'historique**. Constat : `entretiens_vehicules`/`assurances_vehicules` n'ont **AUCUNE FK** vers `vehicules` (donc pas de `ON DELETE CASCADE`) → orphelins possibles. Corrigé : `delete` (repo Drizzle) vérifie `ownsVehicule` puis, **dans la transaction `withTenant`**, supprime entretiens + assurances + véhicule (atomique). Fake repo aligné. **Validé PG** (`vehicule-delete-cascade.test.ts` 2/2 ; suite src 70/70) : delete owné supprime véhicule **+ historique** (0 orphelin) ; delete cross-tenant refusé (véhicule de A + historique intacts). tsc src vert.

- **P1 vehicules 6/9 (OPE-213) FAIT** (2026-06-13) : **use-cases métier (kilométrage/entretiens/assurances) + isolation cross-tenant systématique par sous-ressource**. Les use-cases (enregistrerKilometrage, ajouterEntretien/listEntretiens, ajouterAssurance/listAssurances, scopés via le véhicule parent owné) ont été livrés en 4-5 ; cette étape ajoute la **couverture e2e d'isolation exhaustive** : `vehicules.isolation.test.ts` applique, en tant que tenant B sur la ressource de A, **toutes les routes by-id** du routeur (getById/update/delete/updateKilometrage/addEntretien/addAssurance → **404** ; listEntretiens/listAssurances → **[] (200)**) + vérifie que le véhicule de A reste **intact**. **Validé PG** (9/9 ; suite src 79/79). tsc src vert.

- **P1 vehicules 7a/9 (OPE-214, en cours) FAIT** (2026-06-13) : **alignement parité routeur sur le legacy + lectures dérivées flotte**. Comparé le nouveau routeur au legacy `vehiculesRouter` (server/routers.ts). Aligné les noms : `listEntretiens`→**`getEntretiens`**, `listAssurances`→**`getAssurances`**. Ajouté **`getEntretiensAVenir`** (entretiens de la flotte avec prochaine échéance ≥ aujourd'hui) et **`getAssurancesExpirant`** (assurances dont dateFin ∈ [today, today+N j], N=30 par défaut) — port + repo Drizzle (join `vehicules` pour le scope tenant + RLS) + fake + router. **Validé PG** (`vehicule-derived-reads.test.ts` 2/2 + isolation 9/9 mise à jour ; suite src 81/81) : entretiens à venir/assurances expirant scopés au tenant (B isolé), fenêtre N respectée. tsc src vert.
  - **Reste pour parité complète (OPE-214) → 7b** : `addKilometrage` (legacy **enregistre un historique** via table `historique_kilometrage` + maj du compteur) vs mon `updateKilometrage` (maj compteur seul) ; `getHistoriqueKilometrage` (nécessite modéliser/lire `historique_kilometrage`) ; `getStatistiquesFlotte` (agrégation flotte). → modéliser la table historique kilométrage + ces 3 procédures.

- **P1 vehicules 7b/9 (OPE-214) FAIT** (2026-06-13, OPE-214 = Done) : **kilométrage historique + stats flotte → PARITÉ COMPLÈTE**. Types domaine `ReleveKilometrage`/`CreateKilometrageInput`/`StatistiquesFlotte`. Port+repo+fake : `addKilometrage` (insert `historique_kilometrage` + maj compteur `GREATEST` dans la transaction `withTenant`, scopé véhicule owné), `getHistoriqueKilometrage` (tri date desc, owné), `getStatistiquesFlotte` (nbVehicules/nbActifs/nbEnMaintenance/kmTotalFlotte/coûtEntretiens année courante/assurancesAExpirer 60j — agrégats inline dans 1 tx, scopé tenant). Use-case `enregistrerReleveKilometrage` (valide km≥0). Router : +`addKilometrage`/`getHistoriqueKilometrage`/`getStatistiquesFlotte`. **Validé PG** (`vehicule-kilometrage-stats.test.ts` 2/2 ; suite src 83/83) : historique enregistré + compteur non régressif + isolation B, stats agrégées scopées (B isolé). tsc src vert.
  - **→ PARITÉ DE SIGNATURES ATTEINTE** : le nouveau routeur expose **les 14 procédures du legacy** `vehiculesRouter` (+ `updateKilometrage` en extra). OPE-214 (« parité de signatures avec l'ancien vehiculesRouter ») **= Done**.

- **P1 vehicules 8/9 (OPE-215) FAIT** (2026-06-13) : **harnais d'isolation sur TOUTES les routes by-id + lectures flotte**. `vehicules.isolation.test.ts` étendu : 10 routes by-id testées en cross-tenant (getById/update/delete/updateKilometrage/addEntretien/addAssurance/**addKilometrage** → 404 ; getEntretiens/getAssurances/**getHistoriqueKilometrage** → [] 200) + véhicule de A intact + **lectures flotte de B ne fuient pas A** (getStatistiquesFlotte nbVehicules=0, getEntretiensAVenir=[], getAssurancesExpirant=[]). **Validé PG** (12/12 ; suite src 86/86). tsc src vert.

**→ 🎯 Domaine vehicules COMPLET (gabarit 1→8/9)** : scaffold + port, repo Drizzle (RLS+filtre), use-cases lecture/écriture, routeur tRPC complet (parité legacy 14 procédures), delete cascade, kilométrage historique, stats flotte, **isolation cross-tenant exhaustive prouvée à toutes les couches**. Il ne reste que 9/9 = **bascule flag** (OPE-216, volet infra/déploiement).

- **2e domaine `avis` — étape 1/9 (scaffold + port) FAIT** (2026-06-13) : choisi **`avis`** (avis_clients, table RLS, legacy `avisRouter` à migrer) plutôt que support (pas de table). `src/modules/avis/` : `domain/avis.ts` (types `Avis`, `AvisStats` {moyenne/total/distribution 1-5}, enum statut en_attente/publie/masque, découplés Drizzle), `application/avis-repository.ts` (**port `IAvisRepository`** : list/getById/getStats/**repondre**/**changerStatut** — chaque méthode exige `TenantContext`), `avis.module.ts` (DI). **Validé** (`avis.module.test.ts` 2/2) : stub satisfait le port + module câble. tsc src vert. NB : le legacy avisRouter a aussi `envoyerDemande`/`envoyerDemandeParClient` (envoi email via demandes_avis + EmailPort) — workflow séparé, traité plus tard (étape « logique métier spécifique »).

- **2e domaine `avis` — étape 2/9 (Repository Drizzle scopé tenant) FAIT** (2026-06-13) : `src/modules/avis/infra/avis-repository-drizzle.ts` — `AvisRepositoryDrizzle implements IAvisRepository`, double cloisonnement `withTenant(ctx)` (RLS rôle app) **ET** filtre explicite `artisanId` sur chaque requête (table `avis_clients`). `list` (order createdAt desc, id desc), `getById`, `getStats` (agrégats `moyenne`/`total`/`distribution 1-5` en SQL `group by note`, **avis publiés uniquement**, moyenne arrondie 1 décimale), `repondre` (set reponseArtisan + reponseAt = now, scopé → null hors tenant), `changerStatut` (scopé → null hors tenant). Mapper `toAvis` row↔domaine. **Validé PG (rôle app_tenant + RLS)** (`avis-repository-drizzle.test.ts` 4/4) : list/getById scopés ; **isolation cross-tenant** (B ne lit ni ne modifie l'avis de A : getById→denied via harnais, list exclut, repondre/changerStatut→null, l'avis de A reste intact) ; getStats scopé (avis masqué exclu, tenant B non compté, distribution/moyenne exactes). tsc src vert. (Run PG : `DATABASE_URL=postgres://artisan_user:artisan_password@postgres:5432/artisan_mvp` — l'`APP_DATABASE_URL` est dérivée vers `app_tenant`.) Barrel inchangé (pas d'export infra → pas de couplage DB).

- **2e domaine `avis` — étape 3/9 (use-cases de lecture) FAIT** (2026-06-13) : `src/modules/avis/application/read-use-cases.ts` — fonctions pures (repo injecté) `listAvis(repo, ctx)`, `getAvis(repo, ctx, id)` (→ `NotFoundError` si null, ne révèle pas l'existence cross-tenant), `getAvisStats(repo, ctx)`. + `infra/avis-repository-fake.ts` (`FakeAvisRepository`, in-memory scopé artisanId, miroir du port + `seed()` utilitaire de test hors contrat — le port n'a pas de create, les avis naissent côté portail public). **Validé** (`read-use-cases.test.ts` 5/5) : list scopé, getAvis tenant OK / autre tenant → NotFound (+ harnais `expectCrossTenantDenied`) / id inexistant → NotFound, getStats scopé (masqué exclu, tenant B isolé). tsc src vert. Suite avis 11/11.

- **2e domaine `avis` — étape 4/9 (use-cases d'écriture) FAIT** (2026-06-13) : `src/modules/avis/application/write-use-cases.ts` — fonctions pures async (repo injecté) `repondreAvis(repo, ctx, id, reponse)` (trim + `ValidationError` si vide ; `NotFoundError` si repo→null = hors tenant/inexistant) et `changerStatutAvis(repo, ctx, id, statut)` (`ValidationError` si statut hors union en_attente/publie/masque ; `NotFoundError` si null). Invariant : modération scopée au tenant propriétaire (jamais l'avis d'un autre artisan). **Validé** (`write-use-cases.test.ts` 6/6) : réponse OK (trim), réponse vide → ValidationError, réponse cross-tenant → NotFound (+ harnais `expectCrossTenantDenied`, avis de A intact), statut OK, statut invalide → ValidationError, statut cross-tenant → NotFound (avis de A intact). tsc src vert. Suite avis 17/17.

- **2e domaine `avis` — étape 5/9 (routeur tRPC, parité avisRouter) FAIT** (2026-06-13) : `src/modules/avis/interface/trpc/avis.router.ts` — `createAvisRouter(repo)` (repo injecté), transport mince sur `protectedProcedure`. Procédures parité legacy `avisRouter` (server/routers.ts) : `list` + `getAll` (alias → `listAvis`), `getStats` (→ `getAvisStats`), `getById` (→ `getAvis`), `repondre` (input `{avisId, reponse:string min1 max5000}` → `repondreAvis`), `moderer` (input `{avisId, statut: publie|masque}` → `changerStatutAvis`). Zod valide les inputs ; Domain errors remontent (NotFound→404, Validation→400). **Câblé** dans `createAppRouter` (`avis: createAvisRouter(deps.avisRepo)`) + `buildApp`/`AppDeps` (`avisRepo?` injectable, défaut `AvisRepositoryDrizzle(getDbHandle())`). **Validé e2e PG (HTTP→tRPC→use-case→repo→RLS)** (`avis.router.test.ts` 5/5) : 401 sans cookie, list+getStats scopés A, repondre+moderer par le propriétaire, **isolation cross-tenant** (B : getById 404, list exclut, repondre/moderer 404), validation zod (réponse vide → 400). tsc src vert. **Suite src complète : 108/108 (24 fichiers)**, zéro régression sur le changement de signature `buildApp`. NB : `getAll`/`list` legacy enrichissaient client+intervention — enrichissement = lecture composite à porter via read-model ultérieurement (transport reste mince ici). Le workflow email `envoyerDemande`/`envoyerDemandeParClient` reste pour l'étape 7.

- **2e domaine `avis` — étape 6/9 (e2e isolation exhaustive + read-model enrichissement) FAIT** (2026-06-13) : **(a) read-model d'enrichissement porté** — `AvisEnrichi` (domain) = `Avis` + `client: AvisClientResume|null` + `intervention: AvisInterventionResume|null` ; port `listEnrichi(ctx)` ; `AvisRepositoryDrizzle.listEnrichi` via **leftJoin scopés tenant** (`clients`/`interventions` joints sur `id = avisClients.<fk> AND artisanId = ctx.artisanId`, sous RLS sur les 3 tables → un avis ne peut jamais exposer le client/intervention d'un autre artisan) ; `FakeAvisRepository` (seedClient/seedIntervention + listEnrichi) ; use-case `listAvisEnrichi`. Router `list`/`getAll` renvoient désormais l'**enrichi** (parité legacy getAll/list). **(b) cas e2e/unitaires ajoutés** : enrichissement (client lié joint), getById id inexistant même tenant → 404, moderer statut hors union (en_attente) → 400 (zod parité publie|masque), isolation des jointures (unit : client d'un autre tenant au même id n'est pas joint ; e2e : B ne voit pas l'avis de A). **Validé** : `read-use-cases.test.ts` 6/6, `avis.router.test.ts` 7/7 (e2e PG). tsc src vert. **Suite src complète : 111/111 (24 fichiers)**. Module test stub + clé du port mis à jour (listEnrichi).

- **2e domaine `avis` — étape 7a/9 (workflow demande d'avis : ports + use-cases + fakes) FAIT** (2026-06-13) : kernel effet de bord — **`RateLimiterPort`** (`shared/ports/rate-limiter.ts`, `check(key)→bool`) + `FakeRateLimiter` (denyKey) + barrel ; **`TooManyRequestsError`** (`shared/errors.ts`, code TOO_MANY_REQUESTS) mappée dans le middleware tRPC `mapDomainErrors`. Domaine : `domain/demande-avis.ts` (`DemandeAvis`, `InterventionRef`, `ClientRef`, `CreerDemandeInput`). Port `IDemandeAvisRepository` (`getInterventionOwned`/`getClientOwned`/`getDerniereInterventionDuClient` **scopés tenant → null cross-tenant = NOT_FOUND uniforme anti-oracle** ; `creerDemande`). Use-cases `demande-avis-use-cases.ts` : `envoyerDemandeAvis(deps, ctx, interventionId)` + `envoyerDemandeAvisParClient(deps, ctx, clientId)` — deps injectées (`repo`, `email` EmailPort, `rateLimiter`, `lienBaseUrl` source de confiance APP_URL, `genererToken`/`maintenant` injectables). Logique : ownership (NotFound) → email présent (Validation) → rate limit `avis:<artisanId>` (TooMany) → token uuid + expiry +14j → `creerDemande` → `email.send` (lien `${base}/avis/${token}`). `FakeDemandeAvisRepository` (seedClient/seedIntervention). **Validé** (`demande-avis-use-cases.test.ts` 7/7) : envoi OK (1 email, lien token, expiry +14j, clé rate-limit), intervention cross-tenant → NotFound (+ harnais, 0 effet), client sans email → Validation (0 envoi), rate limit → TooMany (0 effet), ParClient prend la dernière intervention, ParClient cross-tenant → NotFound, ParClient sans intervention → Validation. tsc src vert. **Suite src complète : 118/118 (25 fichiers)**.

- **2e domaine `avis` — étape 7b/9 (DemandeAvisRepositoryDrizzle + câblage routeur + e2e) FAIT** (2026-06-13) : `infra/demande-avis-repository-drizzle.ts` — `DemandeAvisRepositoryDrizzle` via `withTenant(ctx)` + filtre `artisanId` : `getInterventionOwned`/`getClientOwned` (id+artisanId → null sinon), `getDerniereInterventionDuClient` (clientId+artisanId, order dateDebut desc limit 1), `creerDemande` (insert demandes_avis statut envoyee). **`SlidingWindowRateLimiter`** (impl réelle de prod du nouveau stack, fenêtre glissante en mémoire, défaut 5/10min, horloge injectable) ajoutée à `shared/ports/rate-limiter.ts`. Routeur : procédures `envoyerDemande` ({interventionId}) et `envoyerDemandeParClient` ({clientId}) → use-cases ; `createAvisRouter(repo, demandeDeps)`. Câblage `createAppRouter`/`buildApp`/`AppDeps` : `demandeAvisDeps = { repo: DemandeAvisRepositoryDrizzle (défaut), email: LegacyEmailAdapter (défaut, import non littéral), rateLimiter: SlidingWindowRateLimiter (défaut), lienBaseUrl: APP_URL }` — tous injectables (tests : fakes). **Validé e2e PG (workflow ownership tenant + RLS)** (`demande-avis.router.test.ts` 5/5, email+rate-limiter fakes, repo Drizzle réel) : envoi OK (demande persistée + 1 email lien token), B sur l'intervention de A → **404 anti-oracle (0 envoi)**, client sans email → 400 (0 envoi), ParClient (dernière intervention) OK, rate limit → **429**. tsc src vert. **Suite src complète : 123/123 (26 fichiers)**. ⇒ **parité fonctionnelle complète du domaine avis atteinte** (lecture/stats/réponse/modération/enrichissement/workflow demande d'avis).

- **2e domaine `avis` — étape 8/9 (wiring module complet + revue d'invariants + barrel) FAIT** (2026-06-13) : (a) `avis.module.ts` — `createAvisModule({ avisRepo, demande })` assemble et **expose le routeur tRPC prêt** (`module.router`) ; `app.ts` construit le module et passe `module.router` à `createAppRouter` (qui prend désormais `avis: AvisModule` au lieu de repo+deps bruts) → composition découplée des détails d'instanciation du domaine. (b) `index.ts` (barrel) — exporte le **contrat public** : domain (`avis`, `demande-avis`) + ports (`avis-repository`, `demande-avis-repository`) + module/factory ; **pas** les impl Drizzle (zéro couplage DB pour les consommateurs purs). (c) `avis.module.test.ts` réécrit (3/3) : câblage des deps + introspection du routeur (8 procédures : list/getAll/getById/getStats/repondre/moderer/envoyerDemande/envoyerDemandeParClient). tsc src vert. **Suite src complète : 124/124 (26 fichiers)**.
  - **Revue d'invariants du domaine avis (validée par tests)** :
    1. **Isolation cross-tenant** — double cloisonnement partout : RLS (rôle `app_tenant` non-superuser + `app.tenant` GUC via `withTenant`) **ET** filtre explicite `artisanId` dans chaque requête (avis, jointures clients/interventions, demandes). Prouvé : repo 4/4, use-cases lecture/écriture, e2e routeur (B→404/list exclut), jointures enrichies (client d'un autre tenant jamais joint), workflow demande (ownership→404).
    2. **Anti-oracle d'énumération** — ressource hors tenant ⇒ `NotFoundError` → **404 uniforme** (jamais 403 distinct « existe mais pas à toi »). getById/repondre/moderer/envoyerDemande(ParClient).
    3. **0-effet de bord sur échec** — dans le workflow d'envoi, ownership + email présent + rate limit sont vérifiés **avant** tout write/email ; échec ⇒ 0 demande créée, 0 email (prouvé : tests rate-limit 429 et validation 400 asserent `repo.demandes`/`email.sent` inchangés).
    4. **Source de confiance pour le lien** — `lienBaseUrl` vient d'`APP_URL` (injecté), jamais du header `Origin` (non contrôlable par l'appelant).
    5. **Stats publiques cohérentes** — `getStats` ne compte que les avis `publie` (un avis masqué/en attente n'altère pas la note moyenne affichée).

- **2e domaine `avis` — étape 9/9 (bascule flag gateway, volet code) FAIT** (2026-06-13) : le gateway est **domaine-agnostique** (`shouldRouteToNewStack`/`isMigrated` lisent `flags[domain]`, `domainFromTrpcPath` extrait n'importe quel préfixe) → `avis.*` routable **sans changement de logique**. Ajout du **registre `src/interface/gateway/migrated-domains.ts`** (`MIGRATED_DOMAINS = ["vehicules","avis"]` + `isMigratedDomainAvailable`, exporté du barrel gateway) comme garde-fou : un flag ne devrait cibler qu'un domaine réellement monté. **Validé** : `gateway.test.ts` (13 tests) — bascule avis (OFF défaut / canary allowlist / enabled+denylist), extraction `avis.envoyerDemandeParClient`/`avis.moderer`, parse env `NEW_STACK_DOMAINS`/`NEW_STACK_CANARY_AVIS`, registre (avis+vehicules éligibles, factures non) ; `app.test.ts` (7 tests) — **garde-fou de montage** : chaque domaine de `MIGRATED_DOMAINS` répond `401` sur `<domaine>.list` (procédure protégée présente, ≠ 404), un domaine non migré (`factures.list`) → 404. tsc src vert. **Suite src complète : 131/131 (26 fichiers)**. ⇒ **Gabarit avis 9/9 (volet code) TERMINÉ** — 2e domaine clean-archi livré bout-en-bout. **Volet infra réel (déféré, groupé)** : la bascule en prod (proxy Cloudflare `functions/api/[[path]].js` + flag `avis` canary→cutover) requiert le déploiement du stack **Fastify sur staging** + `app_tenant`/RLS staging + `APP_DATABASE_URL` — à grouper avec vehicules **OPE-216** dans un créneau infra dédié.

- **3e domaine `badges` — étape 1/9 (scaffold + port) FAIT** (2026-06-13, epic **OPE-217** techniciens/badges → In Progress) : `src/modules/badges/` — `domain/badge.ts` (`Badge`, `BadgeTechnicien`, `BadgeCategorie` interventions|avis|ca|anciennete|special, Create/Update inputs, découplés Drizzle), `application/badge-repository.ts` (**port `IBadgeRepository`** : list/getById/create/update/delete + `listBadgesTechnicien`/`attribuer` — chaque méthode exige `TenantContext`), `badges.module.ts` (factory `createBadgesModule`), `index.ts` (barrel public sans infra). **Deux niveaux d'isolation documentés dans le port** : `badges` a un `artisanId` (RLS + filtre) ; **`badges_techniciens` n'en a PAS** → scope via l'appartenance du technicien + du badge au tenant (**anti-IDOR**, classe `badges-gamification-idor` historique). **Validé** (`badges.module.test.ts` 2/2 : stub satisfait le port + module câble). tsc src vert. Legacy `badgesRouter` (server/routers.ts L7442) à migrer : list/create/update(assertBadgeOwner)/delete/getBadgesTechnicien(assertTechnicienOwner)/attribuerBadge/verifierBadges/getClassement/calculerClassement — `verifierBadges`/`getClassement` = logique métier dérivée (étapes ultérieures).

- **3e domaine `badges` — étape 2/9 (Repository Drizzle scopé tenant) FAIT** (2026-06-13) : `src/modules/badges/infra/badge-repository-drizzle.ts` — `BadgeRepositoryDrizzle` via `withTenant(ctx)`. **`badges`** : double cloisonnement RLS + filtre `artisanId` (list order createdAt desc, getById, create artisanId forcé, update/delete scopés → null/false ; delete = cascade applicative `badges_techniciens` après vérif ownership). **`badges_techniciens`** (pas d'artisanId) : `listBadgesTechnicien`/`attribuer` vérifient l'appartenance du **technicien** (`ownsTechnicien` via `techniciens.id+artisanId`, table sous RLS) ET du **badge** (`ownsBadge`) AVANT tout accès → `[]`/`null` sinon (**anti-IDOR sur les 2 FK**). `attribuer` **idempotent** : si (technicienId,badgeId) existe, renvoie l'existant. Mappers row↔domaine. **Validé PG (rôle app_tenant + RLS)** (`badge-repository-drizzle.test.ts` 4/4) : CRUD scopés ; **isolation cross-tenant** (B ne lit/modifie/supprime pas le badge de A, badge de A intact) ; attribution idempotente (2e appel = même ligne) ; **anti-IDOR** (A→technicien de B = null, B→badge de A = null, B ne lit pas les badges du technicien de A). tsc src vert. Suite badges 6/6.

- **3e domaine `badges` — étape 3/9 (use-cases de lecture) FAIT** (2026-06-13) : `src/modules/badges/application/read-use-cases.ts` — purs (repo injecté) `listBadges`, `getBadge` (→ `NotFoundError` si null, ne révèle pas l'existence cross-tenant), `listBadgesDuTechnicien` (liste, `[]` si technicien hors tenant — anti-IDOR, pas d'oracle). + `infra/badge-repository-fake.ts` (`FakeBadgeRepository` in-memory scopé artisanId + `seedTechnicien(id, artisanId)` ; reproduit l'anti-IDOR : `attribuer`/`listBadgesTechnicien` refusent un technicien non déclaré pour le tenant, et `attribuer` exige le badge dans le tenant ; idempotent). **Validé** (`read-use-cases.test.ts` 6/6) : list scopé, getBadge tenant OK / autre tenant → NotFound (+ harnais) / inexistant → NotFound, listBadgesDuTechnicien scopé + technicien d'un autre tenant → []. tsc src vert. Suite badges 12/12.

- **3e domaine `badges` — étape 4/9 (use-cases d'écriture) FAIT** (2026-06-13) : `src/modules/badges/application/write-use-cases.ts` — purs async (repo injecté) `creerBadge` (valide code+nom non vides → `ValidationError`), `modifierBadge`/`supprimerBadge` (→ `NotFoundError` si repo null/false = hors tenant/inexistant), `attribuerBadge` (→ `NotFoundError` si repo.attribuer null = technicien OU badge hors tenant ; **anti-IDOR sur les 2 FK** ; idempotent par le repo). **Validé** (`write-use-cases.test.ts` 7/7) : create OK, code/nom vide → ValidationError, modifier OK / cross-tenant → NotFound (+ harnais), supprimer OK / cross-tenant → NotFound / déjà supprimé → NotFound, attribuer OK + idempotent, attribuer technicien d'un autre tenant → NotFound (+ harnais), attribuer badge d'un autre tenant → NotFound. tsc src vert. Suite badges 19/19.

- **3e domaine `badges` — étape 5/9 (routeur tRPC + câblage + e2e) FAIT** (2026-06-13) : `src/modules/badges/interface/trpc/badges.router.ts` — `createBadgesRouter(repo)`, transport mince. Procédures parité legacy `badgesRouter` : `list`, `create`, `update` ({id,data}), `delete` ({id}→{success}), `getBadgesTechnicien` ({technicienId}), `attribuerBadge` ({technicienId,badgeId,valeurAtteinte?}). **Bornes zod alignées table** (code≤50, nom≤100, icone≤50, couleur≤20, description≤2000 ; categorie enum). Domain errors → 404/400. **`createBadgesModule` enrichi** (expose `.router`) + monté dans `createAppRouter` (`badges: deps.badges.router`) + `buildApp`/`AppDeps` (`badgeRepo?` injectable défaut `BadgeRepositoryDrizzle`). **`badges` ajouté au registre `MIGRATED_DOMAINS`** (+ `sampleProcedure` app.test). **Validé e2e PG** (`badges.router.test.ts` 5/5) : 401 sans cookie, create+list scopés A, validation zod (code vide → 400), **isolation cross-tenant** (B update/delete badge de A → 404), **anti-IDOR** (attribuerBadge OK technicien de A ; A→technicien de B → 404 ; getBadgesTechnicien scopé). `badges.module.test` 3/3 (introspection 6 procédures). tsc src vert. **Suite src complète : 157/157 (31 fichiers)**. NB : `verifierBadges`/`getClassement`/`calculerClassement` = logique dérivée (étape 7).

- **3e domaine `badges` — étape 6/9 (couverture e2e isolation exhaustive + bornes) FAIT** (2026-06-13) : `badges.router.test.ts` étendu (5→10 e2e) — `getBadgesTechnicien` sur un technicien d'un autre tenant → **`[]` (200, lecture sans oracle, pas 404)** ; `attribuerBadge` avec un **badge** d'un autre tenant → 404 (anti-IDOR 2e FK) ; `update`/`delete` sur id inexistant du même tenant → 404 ; bornes zod (nom > 100 → 400) ; **idempotence e2e** (double `attribuerBadge` → même id, `getBadgesTechnicien` longueur 1). tsc src vert. Suite badges 24 (10 e2e + 4 repo PG + 6 read + 7 write + 3 module − overlap) ; **suite src complète : 162/162 (31 fichiers)**.

- **3e domaine `badges` — étape 7a/9 (logique dérivée : classement LECTURE) FAIT** (2026-06-13) : porté `getClassement` (parité legacy `db.getClassementTechniciens`, lecture pure). `domain/classement.ts` (`ClassementEntry` : rang/pointsTotal/interventions/ca/noteMoyenne, montants en string PG ; `PeriodeClassement` semaine|mois|trimestre|annee). Port `IBadgeRepository.getClassement(ctx, periode)`. `BadgeRepositoryDrizzle.getClassement` : withTenant + filtre `artisanId` sur `classement_techniciens` (table sous RLS), order rang asc. `FakeBadgeRepository.getClassement` + `seedClassement`. Use-case `getClassementTechniciens(repo, ctx, periode)`. Routeur : procédure `getClassement` ({periode enum}). **Validé** : `read-use-cases.test.ts` 7/7 (classement scopé tenant + ordonné rang, autre période → [], tenant B isolé) ; `badges.router.test.ts` 11/11 (e2e PG : seed classement A+B via admin, A ne voit que son classement, B isolé) ; module test 3/3 (7 procédures). tsc src vert. **Suite src complète : 164/164 (31 fichiers)**.

- **3e domaine `badges` — étape 7b/9 (logique dérivée ÉCRITURE : calculerClassement) FAIT** (2026-06-13) : porté `recalculerClassement` (parité legacy `db.calculerClassement`). `BadgeRepositoryDrizzle.recalculerClassement` dans **une seule transaction** (`withTenant`) : bornes de période (`bornesPeriode`, horloge injectable au constructeur pour déterminisme), agrégat `interventions` **`terminee`** de la période (`dateDebut::date between debut/fin` — cast date pour inclure le jour courant) **LEFT JOIN `factures` payées** (CA = `sum(totalTTC)`, condition `statut='payee'` dans le ON), `points = nb*10 + floor(ca/100)`, **purge** (artisanId,periode,dateDebut) puis **insert** ranké (count desc, ca desc), relecture dans la même tx. **Tout scopé tenant** (`interventions.artisanId = ctx.artisanId` + RLS sur interventions/factures/classement). Fake = recompute no-op (renvoie le seedé). Use-case `calculerClassement` + procédure routeur `calculerClassement` (mutation). **Validé e2e PG** (`badges.router.test.ts` 12/12) : recalcul A (techA 2 interventions + CA 500 → rang 1, points 25 ; tech2 1 interv → 10 ; **intervention de B ignorée** ; techB absent du classement de A). tsc src vert. **Suite src complète : 165/165 (31 fichiers)**.

- **3e domaine `badges` — étape 7c/9 (logique dérivée : verifierBadges auto-attribution) FAIT** (2026-06-13) : porté `verifierEtAttribuerBadges` (parité legacy `db.verifierEtAttribuerBadges`). `BadgeRepositoryDrizzle.verifierEtAttribuerBadges` en **une transaction** : **ownership technicien d'abord** (anti-IDOR → `null` si hors tenant) → agrégats scopés tenant (interventions `terminee` du technicien ; avis `note≥4` de l'artisan) → pour chaque badge **actif** du tenant avec `seuil>0`, `valeur` = (categorie interventions → nbInterventions ; avis → nbAvisPositifs), si `valeur≥seuil` → attribution **idempotente** (upsert dans la même tx). Fake : `seedProgress(technicienId, {interventions, avisPositifs})` + même logique. Use-case `verifierBadges` (→ `NotFoundError` si null = anti-IDOR). Procédure routeur `verifierBadges` ({technicienId}). **Validé** : `write-use-cases.test.ts` 10/10 (seuil atteint → attribution + idempotent, non atteint → [], technicien hors tenant → NotFound + harnais) ; `badges.router.test.ts` 13/13 (e2e PG : 2 interventions terminées → badge seuil 2 attribué, unique ; A→technicien de B → 404 ; B→technicien de A → 404). tsc src vert. **Suite src complète : 169/169 (31 fichiers)**. ⇒ **logique métier dérivée du domaine badges complète** (classement lecture+recalcul, vérification/auto-attribution).

- **3e domaine `badges` — étape 8/9 (revue d'invariants + barrel complet) FAIT** (2026-06-13) : (a) barrel `index.ts` complété (`domain/classement` exporté ; toujours **sans impl Drizzle** → zéro couplage DB) + `index.test.ts` (contrat public : `createBadgesModule` réexporté). Wiring déjà fait à l'étape 5 (`createBadgesModule.router` monté dans `createAppRouter`/`buildApp`, `badges` au registre `MIGRATED_DOMAINS`). tsc src vert ; suite badges 38/38.
  - **Revue d'invariants du domaine badges (validée par tests)** :
    1. **Isolation cross-tenant** — double cloisonnement RLS (rôle `app_tenant` + GUC `app.tenant` via `withTenant`) **ET** filtre `artisanId` partout : `badges` (CRUD), `classement_techniciens` (lecture + recalcul), et tous les agrégats (interventions/factures/avis). Prouvé : repo 4/4, read 7/7, write 10/10, e2e routeur 13/13.
    2. **Anti-IDOR `badges_techniciens`** (table SANS `artisanId`) — ownership vérifié AVANT tout accès, sur les **2 FK** : technicien (`techniciens.id+artisanId`) ET badge (`badges.id+artisanId`). Lecture `getBadgesTechnicien` d'un technicien hors tenant → `[]` (200, **sans oracle**) ; écritures (`attribuer`/`verifierBadges`) hors tenant → `null` → **404 uniforme**.
    3. **Idempotence** — `attribuer` (upsert (technicienId,badgeId)), `verifierBadges` (réutilise l'attribution existante), `recalculerClassement` (purge (artisan,periode,debut) avant insert). Double appel ⇒ état stable (prouvé e2e).
    4. **Recalcul/vérif scopés tenant** — `calculerClassement` n'agrège que les interventions `terminee` + factures `payee` du tenant (e2e : intervention d'un autre artisan ignorée) ; `verifierBadges` n'agrège que les interventions/avis du tenant.
    5. **Bornes d'entrée** — zod aligné sur la table (`code≤50`, `nom≤100`, `icone≤50`, `couleur≤20`, `description≤2000`) ; `creerBadge` exige code+nom (ValidationError → 400).

- **3e domaine `badges` — étape 9/9 (bascule flag gateway, volet code) FAIT** (2026-06-13) : `gateway.test.ts` étendu (13→16) — cas explicites domaine `badges` (OFF défaut / canary allowlist / enabled+denylist ; extraction `badges.attribuerBadge`/`badges.calculerClassement` ; parse env `NEW_STACK_DOMAINS`/`NEW_STACK_CANARY_BADGES`) + registre (badges éligible). Volet code déjà en place (étape 5 : `badges` dans `MIGRATED_DOMAINS`, gateway domaine-agnostique, garde-fou de montage `app.test` badges.list→401). tsc src vert. **Suite src complète : 173/173 (32 fichiers)**. ⇒ **Gabarit badges 9/9 (volet code) TERMINÉ — 3e domaine clean-archi livré bout-en-bout** (CRUD + attribution anti-IDOR + classement lecture/recalcul + auto-attribution badges). **OPE-217 → Done**. **Volet infra réel (déféré, groupé)** : bascule prod (proxy Cloudflare + flag `badges` canary→cutover) avec vehicules+avis sous **OPE-216** (déploiement Fastify staging + `app_tenant`/RLS + `APP_DATABASE_URL`).

- **4e domaine `techniciens` — étape 1/9 (scaffold + port) FAIT** (2026-06-13, **issue dédiée OPE-256** créée → In Progress ; périmètre techniciens repris de l'epic OPE-217 clos sur badges) : `src/modules/techniciens/` — `domain/technicien.ts` (`Technicien`, `TechnicienStatut` actif|inactif|conge, Create/Update inputs, coutHoraire en string PG, userId lié optionnel), `application/technicien-repository.ts` (**port `ITechnicienRepository`** : list/getById/create/update/delete, chaque méthode exige `TenantContext`), `techniciens.module.ts` (factory `createTechniciensModule`), `index.ts` (barrel public sans infra). `techniciens` a un `artisanId` → RLS + filtre ; sous-ressources positions/disponibilites/objectifs (SANS artisanId) → scope via ownership technicien (anti-IDOR géoloc, étapes ultérieures). **Validé** (`techniciens.module.test.ts` 2/2 : stub satisfait le port + module câble). tsc src vert. Legacy `techniciensRouter` (server/routers.ts L5727) à migrer : getAll/getById/create/update/delete/getLinkableUsers/getDisponibles + endpoints positions/disponibilités.

- **4e domaine `techniciens` — étape 2/9 (Repository Drizzle scopé tenant) FAIT** (2026-06-13) : `src/modules/techniciens/infra/technicien-repository-drizzle.ts` — `TechnicienRepositoryDrizzle` via `withTenant(ctx)` + filtre `artisanId` (table `techniciens`, RLS). `list` (order nom asc), `getById`, `create` (artisanId forcé), `update` (set + updatedAt, scopé → null), `delete` (ownership AVANT cascade applicative → false sinon). **delete cascade** (transaction) : purge `positions_techniciens`/`disponibilites_techniciens`/`badges_techniciens`/`objectifs_techniciens`/`classement_techniciens` (rattachés au technicien ; certains SANS artisanId, d'où ownership d'abord). Mapper row↔domaine (coutHoraire string|null, userId number|null). **Validé PG (rôle app_tenant + RLS)** (`technicien-repository-drizzle.test.ts` 4/4) : CRUD scopés ; **isolation cross-tenant** (B ne lit/modifie/supprime pas le technicien de A, A intact) ; update champs scopés ; **delete cascade** (technicien + disponibilités + positions purgés). tsc src vert. Suite techniciens 6/6.

- **4e domaine `techniciens` — étape 3/9 (use-cases de lecture) FAIT** (2026-06-13) : `src/modules/techniciens/application/read-use-cases.ts` — purs (repo injecté) `listTechniciens`, `getTechnicien` (→ `NotFoundError` si null, ne révèle pas l'existence cross-tenant). + `infra/technicien-repository-fake.ts` (`FakeTechnicienRepository` in-memory scopé artisanId, miroir CRUD du contrat). **Validé** (`read-use-cases.test.ts` 4/4) : list scopé, getTechnicien tenant OK / autre tenant → NotFound (+ harnais) / inexistant → NotFound. tsc src vert. Suite techniciens 10/10.

- **4e domaine `techniciens` — étape 4/9 (use-cases d'écriture) FAIT** (2026-06-13) : `src/modules/techniciens/application/write-use-cases.ts` — purs async (repo injecté) `creerTechnicien` (valide `nom` non vide → `ValidationError`), `modifierTechnicien` (refuse un `nom` explicitement vidé → Validation ; → `NotFoundError` si repo.update null), `supprimerTechnicien` (→ `NotFoundError` si repo.delete false). **Validé** (`write-use-cases.test.ts` 4/4) : create OK, nom vide → ValidationError, modifier OK / nom vidé → Validation / cross-tenant → NotFound (+ harnais), supprimer OK / cross-tenant → NotFound / déjà supprimé → NotFound. tsc src vert. Suite techniciens 14/14.

- **4e domaine `techniciens` — étape 5/9 (routeur tRPC + câblage + e2e) FAIT** (2026-06-13) : `src/modules/techniciens/interface/trpc/techniciens.router.ts` — `createTechniciensRouter(repo)`, transport mince. Procédures parité legacy : `list`/`getAll`, `getById`, `create`, `update` ({id,...} aplati comme le legacy), `delete` ({id}→{success}). **Bornes zod alignées table** (nom≤255, email email+≤320, couleur #RRGGBB|"", coutHoraire regex décimal, statut enum, notes≤5000, userId nullable). Domain errors → 404/400. **`createTechniciensModule` enrichi** (expose `.router`) + monté dans `createAppRouter` + `buildApp`/`AppDeps` (`technicienRepo?` injectable défaut Drizzle). **`techniciens` ajouté à `MIGRATED_DOMAINS`** (+ sampleProcedure app.test). **Validé e2e PG** (`techniciens.router.test.ts` 5/5) : 401 sans cookie, create+list scopés A, validation zod (nom vide → 400, email invalide → 400), **isolation cross-tenant** (B getById/update/delete technicien de A → 404, list vide, A intact), update+delete OK propriétaire. `techniciens.module.test` 3/3 (6 procédures). tsc src vert. **Suite src complète : 194/194 (37 fichiers)**. NB : `getLinkableUsers`/`getDisponibles` + positions/disponibilités = sous-ressources (étape 7).

- **4e domaine `techniciens` — étape 6/9 (couverture e2e isolation exhaustive + bornes) FAIT** (2026-06-13) : `techniciens.router.test.ts` étendu (5→9 e2e) — getById/update/delete sur id inexistant du même tenant → 404 ; bornes zod (nom > 255 → 400, couleur non #RRGGBB → 400, coutHoraire non décimal → 400) ; `getAll` ≡ `list` (parité) ; **update partiel** ne touche pas les champs non fournis (statut modifié, specialite/nom préservés). tsc src vert. **Suite src complète : 198/198 (37 fichiers)**.

- **4e domaine `techniciens` — étape 7a/9 (sous-ressource disponibilités) FAIT** (2026-06-13) : `domain/disponibilite.ts` (`Disponibilite`, `SetDisponibiliteInput`). Port étendu : `listDisponibilites(ctx, technicienId)` (→ `[]` si technicien hors tenant, lecture sans oracle) + `setDisponibilite(ctx, technicienId, input)` (upsert par jourSemaine, → null hors tenant). **`disponibilites_techniciens` n'a PAS d'artisanId** → ownership technicien vérifié AVANT tout accès (`ownsTechnicien`), **anti-IDOR planning/géoloc**. Drizzle (withTenant + ownsTechnicien + upsert) + fake. Use-cases `listDisponibilites`/`definirDisponibilite` (valide jourSemaine 0..6 + heureFin>heureDebut → ValidationError ; NotFound si null). Routeur : `getDisponibilites`/`setDisponibilite` (zod : jourSemaine 0-6, heures HH:MM). **Validé** : read 5/5 (technicien hors tenant → []), write 5/5 (upsert même jour, validations, anti-IDOR → NotFound + harnais), e2e PG 10/10 (set+upsert une seule ligne, validation 400, B→set 404 / get → []), module 3/3 (8 procédures). tsc src vert. **Suite src complète : 201/201 (37 fichiers)**.

- **4e domaine `techniciens` — étape 7b/9 (sous-ressource positions GPS) FAIT** (2026-06-13) : `domain/position.ts` (`Position`, `EnregistrerPositionInput` ; lat/lon en string PG numeric(10,8)/(11,8)). Port étendu : `getDernierePosition(ctx, technicienId)` (→ null hors tenant/aucune) + `enregistrerPosition(ctx, technicienId, input)` (→ null hors tenant). **`positions_techniciens` SANS artisanId** → ownership technicien AVANT tout accès (`ownsTechnicien`), **anti-IDOR géoloc historique**. Drizzle (withTenant + ownsTechnicien ; dernière position par `id desc` — `timestamp` `now()` peut être identique sur insertions rapprochées) + fake. Use-cases `getDernierePosition`/`enregistrerPosition` (valide lat ∈[-90,90], lon ∈[-180,180] → ValidationError ; NotFound si null). Routeur `getDernierePosition`/`enregistrerPosition` (zod : lat/lon string décimal, cap 0-360, batterie 0-100). **Validé** : read 6/6 (aucune position → null, technicien hors tenant → null), write 6/6 (OK, validations plage, anti-IDOR → NotFound + harnais), e2e PG 11/11 (2 positions → dernière renvoyée, validation 400, B → get null / enregistrer 404), module 3/3 (10 procédures). ⚠️ piège testé : `numeric(10,8)` formate `"45.76"`→`"45.76000000"` (assertions e2e via `Number().toBeCloseTo`). tsc src vert. **Suite src complète : 204/204 (37 fichiers)**.

- **4e domaine `techniciens` — étape 7c/9 (getLinkableUsers) FAIT** (2026-06-13) : porté `getLinkableUsers` (parité legacy `db.getUsersByArtisanId`). `domain/utilisateur-liable.ts` (`UtilisateurLiable` {id, nom, email, role}). Port `getUsersLiables(ctx)`. **`users`/`artisans` HORS RLS tenant** → scope par **filtre artisanId EXPLICITE** : `BadgeRepositoryDrizzle`-like — résout `artisans.userId` (propriétaire) puis `users where id = owner OR artisanId = ctx.artisanId` (propriétaire + collaborateurs), mappe `nom = [prenom, name].join || email`. Use-case `listerUtilisateursLiables` + routeur `getLinkableUsers`. Fake `seedUsersLiables`. **Validé** : read 7/7 (scopé tenant), e2e PG 12/12 (A voit propriétaire UA + collaborateur ; PAS UB/collab B ; B réciproque), module 3/3 (11 procédures). tsc src vert. **Suite src complète : 206/206 (37 fichiers)**. ⇒ **sous-ressources techniciens complètes** (disponibilités + positions GPS anti-IDOR + getLinkableUsers).

- **4e domaine `techniciens` — étape 8/9 (barrel complet + revue d'invariants) FAIT** (2026-06-13) : barrel `index.ts` complété (`domain/disponibilite`, `domain/position`, `domain/utilisateur-liable` exportés ; toujours **sans impl Drizzle**) + `index.test.ts` (contrat public `createTechniciensModule`). Wiring déjà en place (étape 5). tsc src vert ; suite techniciens 33/33.
  - **Revue d'invariants du domaine techniciens (validée par tests)** :
    1. **Isolation cross-tenant** — `techniciens` : double cloisonnement RLS (`app_tenant` + GUC `app.tenant`) + filtre `artisanId`. Prouvé repo 4/4, read 7/7, write 6/6, e2e 12/12.
    2. **Anti-IDOR sous-ressources SANS artisanId** — `disponibilites_techniciens`, `positions_techniciens` : ownership technicien (`ownsTechnicien`) vérifié AVANT tout accès. Lecture hors tenant → `[]`/`null` (**sans oracle**) ; écriture hors tenant → `null` → **404**. (Anti-IDOR planning + géoloc, défauts historiques fermés.)
    3. **Scope EXPLICITE hors RLS** — `getLinkableUsers` lit `users`/`artisans` (en **denylist RLS**, pas de policy tenant) → le scope repose sur un **filtre artisanId explicite** (propriétaire + collaborateurs). Prouvé e2e (A ne voit jamais les users d'un autre tenant).
    4. **Cascade delete atomique** — suppression du technicien purge ses sous-ressources (positions/disponibilités/badges/objectifs/classement) dans la même transaction, après ownership.
    5. **Validations** — nom requis (Validation→400), coordonnées (lat[-90,90]/lon[-180,180]), heures (fin>début, HH:MM), bornes zod alignées table (nom≤255, email, couleur #RRGGBB, coutHoraire décimal, cap 0-360, batterie 0-100).

- **4e domaine `techniciens` — étape 9/9 (bascule flag gateway, volet code) FAIT** (2026-06-13) : `gateway.test.ts` étendu (16→19) — cas explicites domaine `techniciens` (OFF défaut / canary allowlist / enabled+denylist ; extraction `techniciens.setDisponibilite`/`techniciens.enregistrerPosition` ; parse env `NEW_STACK_DOMAINS`/`NEW_STACK_CANARY_TECHNICIENS`) + registre (techniciens éligible). Volet code déjà en place (étape 5 : `techniciens` dans `MIGRATED_DOMAINS`, gateway domaine-agnostique, garde-fou de montage `app.test` techniciens.list→401). tsc src vert. **Suite src complète : 210/210 (38 fichiers)**. ⇒ **Gabarit techniciens 9/9 (volet code) TERMINÉ — 4e domaine clean-archi livré bout-en-bout** (CRUD + delete cascade + disponibilités + positions GPS anti-IDOR géoloc + getLinkableUsers scope explicite hors RLS). **OPE-256 → Done**. **Volet infra réel (déféré, groupé)** : bascule prod (proxy Cloudflare + flag `techniciens` canary→cutover) avec vehicules+avis+badges sous **OPE-216**.

- **5e domaine `notifications` — étape 1/9 (scaffold + port) FAIT** (2026-06-13, **issue dédiée OPE-257** → In Progress) : `src/modules/notifications/` — `domain/notification.ts` (`Notification`, `NotificationType` info|alerte|rappel|succes|erreur, `ListNotificationsOptions` {includeArchived, nonLuesUniquement, page, limit}), `application/notification-repository.ts` (**port `INotificationRepository`** : list/countUnread/markAsRead/markAllAsRead/archive ; chaque méthode exige `TenantContext`), `notifications.module.ts` (factory `createNotificationsModule`), `index.ts` (barrel sans infra). `notifications` a un `artisanId` → RLS + filtre ; **IDOR historique** (`notifications-push-router-idor`) sur markAsRead/archive → ownership forcé (false hors tenant). **Validé** (`notifications.module.test.ts` 2/2). tsc src vert. Legacy `notificationsRouter` (server/routers.ts L2513) à migrer : list (paginée/filtrée, offset cap anti-DoS), getUnreadCount, markAsRead, markAllAsRead, archive, delete (=archive), generateOverdueReminders (dérivé : rappels factures impayées).

- **5e domaine `notifications` — étape 2/9 (Repository Drizzle scopé tenant) FAIT** (2026-06-13) : `src/modules/notifications/infra/notification-repository-drizzle.ts` — `NotificationRepositoryDrizzle` via `withTenant(ctx)` + filtre `artisanId` (table `notifications`, RLS). `list(ctx, options)` : conds artisanId + archived=false (sauf includeArchived) + lu=false (si nonLuesUniquement), order createdAt desc, **pagination bornée** (limit clampé 1..100, page 1..100000, offset SQL → anti-DoS). `countUnread` (lu=false + non archivé). `markAsRead(id)`/`archive(id)` (update where id+artisanId → false si 0 ligne). `markAllAsRead` (update lu=true where artisanId + lu=false → count). Mapper `toNotification`. **Validé PG (rôle app_tenant + RLS)** (`notification-repository-drizzle.test.ts` 4/4) : list scopée + filtres (archived/non lues) + countUnread ; pagination bornée (limit/page, clamp limit>100) ; **isolation cross-tenant** (B markAsRead/archive notif de A → false, A intacte) ; markAllAsRead ne touche que le tenant (B intact). tsc src vert. Suite notifications 6/6.

- **5e domaine `notifications` — étape 3/9 (use-cases de lecture) FAIT** (2026-06-13) : `src/modules/notifications/application/read-use-cases.ts` — purs (repo injecté) `listNotifications(repo, ctx, options)` (délègue ; pagination bornée côté repo), `compterNonLues(repo, ctx)`. + `infra/notification-repository-fake.ts` (`FakeNotificationRepository` in-memory scopé artisanId + `seed()` ; reproduit filtres archived/lu + pagination clampée + anti-IDOR markAsRead/archive hors tenant → false). **Validé** (`read-use-cases.test.ts` 4/4) : list scopée (exclut archivées par défaut), filtres non lues/includeArchived, pagination bornée (clamp limit), compterNonLues scopé (tenant B isolé). tsc src vert. Suite notifications 10/10.

- **5e domaine `notifications` — étape 4/9 (use-cases d'écriture) FAIT** (2026-06-13) : `src/modules/notifications/application/write-use-cases.ts` — purs async (repo injecté) `marquerLue(repo, ctx, id)` (→ `NotFoundError` si repo.markAsRead false = hors tenant/inexistant, **anti-IDOR**), `marquerToutesLues(repo, ctx)` (→ nombre marqué), `archiver(repo, ctx, id)` (→ `NotFoundError` si repo.archive false). **Validé** (`write-use-cases.test.ts` 4/4) : marquerLue OK / cross-tenant → NotFound (+ harnais), marquerToutesLues compte (2) + ne touche pas l'autre tenant, archiver OK (exclue de la liste) / cross-tenant → NotFound, id inexistant → NotFound. tsc src vert. Suite notifications 14/14.

- **5e domaine `notifications` — étape 5/9 (routeur tRPC + câblage + e2e) FAIT** (2026-06-13) : `src/modules/notifications/interface/trpc/notifications.router.ts` — `createNotificationsRouter(repo)`, transport mince. Procédures parité legacy : `list` (input optionnel {includeArchived, nonLuesUniquement, page 1..100000, limit 1..100}), `getUnreadCount`, `markAsRead` ({id}→{success}), `markAllAsRead` (→{success,count}), `archive` ({id}→{success}), `delete` (={archive}, alias legacy). Bornes zod alignées (page/limit). Domain errors → 404. **`createNotificationsModule` enrichi** (expose `.router`) + monté dans `createAppRouter` + `buildApp`/`AppDeps` (`notificationRepo?` injectable défaut Drizzle). **`notifications` ajouté à `MIGRATED_DOMAINS`** (+ sampleProcedure app.test). **Validé e2e PG** (`notifications.router.test.ts` 5/5) : 401 sans cookie, list+getUnreadCount scopés A, markAsRead/markAllAsRead/archive propriétaire, **isolation cross-tenant** (B markAsRead/archive/delete notif de A → 404, list exclut), pagination (limit borné, limit>100 → 400 zod). `notifications.module.test` 3/3 (6 procédures). tsc src vert. **Suite src complète : 231/231 (43 fichiers)**. NB : `generateOverdueReminders` = logique dérivée (étape 7).

- **5e domaine `notifications` — étape 6/9 (couverture e2e isolation exhaustive + bornes) FAIT** (2026-06-13) : `notifications.router.test.ts` étendu (5→9 e2e) — markAsRead/archive sur id inexistant du même tenant → 404 ; bornes zod (page 0 → 400, page > 100000 → 400) ; **filtres e2e exacts** (défaut exclut archivées (3) + **ordre createdAt desc** vérifié ; nonLuesUniquement exclut lue+archivée ; includeArchived → 4) ; **markAllAsRead n'affecte pas l'autre tenant** (e2e : A→0, B→1) + getUnreadCount reflète. tsc src vert. **Suite src complète : 235/235 (43 fichiers)**.

- **5e domaine `notifications` — étape 7a/9 (logique dérivée : generateOverdueReminders — port + use-case + Drizzle + fake) FAIT** (2026-06-13) : `domain/facture-en-retard.ts` (`FactureEnRetard` {id, numero, totalTTC, dateEcheance, clientNom}, `CreerNotificationInput`). Port étendu : `listFacturesEnRetard(ctx)` (lecture seule factures, statut ≠ payee/annulee + `dateEcheance::date < today`, scopé artisanId + RLS, leftJoin clients scopé), `existeNotificationActive(ctx, lien)` (idempotence), `creer(ctx, input)` (insert notification scopé). Use-case `genererRappelsFacturesEnRetard(repo, ctx, maintenant?)` : pour chaque facture en retard, **anti-doublon** (skip si rappel actif sur le même `lien`), crée une notification type `rappel` (titre `Facture {numero} en retard`, message client + jours retard + montant, lien `/factures/{id}`) ; renvoie `{rappelsCreated}`. Horloge injectable. **Domaine sensible (facturation) : lecture seule sur factures**, aucune écriture. Drizzle (3 méthodes) + fake (`seedFacturesEnRetard`). **Validé** (`derived-use-cases.test.ts` 5/5) : 1 rappel/facture (message formaté : jours retard exacts, montant, client), **idempotent** (2e passage → 0), **isolation cross-tenant** (A/B indépendants), 0 facture → 0, client null → « Client » + montant 0.00. tsc src vert. **Suite src complète : 240/240 (44 fichiers)**.

- **5e domaine `notifications` — étape 7b/9 (routeur generateOverdueReminders + e2e PG) FAIT** (2026-06-13) : procédure `generateOverdueReminders` (mutation) ajoutée à `createNotificationsRouter` → `genererRappelsFacturesEnRetard(repo, ctx.tenant)` → `{success, rappelsCreated}`. `notifications.module.test` 3/3 (7 procédures). **Validé e2e PG** (`notifications.router.test.ts` 10/10) : seed A (1 facture `envoyee` échéance −10j, 1 `payee` échéance −5j ignorée, 1 `envoyee` échéance +5j ignorée) + B (1 facture en retard) → `generateOverdueReminders` (A) crée **1 rappel** (type `rappel`, lien `/factures/{id}`) ; **idempotence** (2e appel → 0) ; **isolation** (la facture en retard de B ne génère pas de rappel pour A). Nettoyage factures/clients dans before/afterAll. tsc src vert. **Suite src complète : 241/241 (44 fichiers)**. ⇒ **logique métier dérivée notifications complète** (generateOverdueReminders, lecture seule factures, idempotent, scopé tenant).

- **5e domaine `notifications` — étape 8/9 (barrel complet + revue d'invariants) FAIT** (2026-06-13) : barrel `index.ts` complété (`domain/facture-en-retard` exporté ; toujours **sans impl Drizzle**) + `index.test.ts` (contrat public `createNotificationsModule`). Wiring déjà en place (étape 5). tsc src vert ; suite notifications 31/31.
  - **Revue d'invariants du domaine notifications (validée par tests)** :
    1. **Isolation cross-tenant** — `notifications` : double cloisonnement RLS (`app_tenant` + GUC `app.tenant`) + filtre `artisanId` partout (list, count, markAsRead/markAllAsRead/archive, creer, listFacturesEnRetard). Prouvé repo 4/4, read 4/4, write 4/4, derived 5/5, e2e 10/10.
    2. **Anti-IDOR** — markAsRead/archive/delete d'une notification hors tenant → `false` → **404** (jamais d'effet cross-tenant) ; markAllAsRead scopé tenant uniquement.
    3. **Idempotence des rappels** — `generateOverdueReminders` ne recrée pas un rappel si une notification active (non archivée) pointe déjà vers la même facture (`lien`). Prouvé unit + e2e (2e appel → 0).
    4. **Lecture seule sur domaine sensible (facturation)** — la génération de rappels LIT les factures (statut ≠ payee/annulee + échéance dépassée) mais ne les modifie JAMAIS ; seules des notifications sont créées. Filtrage scopé artisanId (factures d'un autre tenant ignorées).
    5. **Pagination/DoS** — list borne limit (1..100) et page (1..100000) → offset SQL plafonné (anti-scan coûteux) ; bornes zod au routeur (limit>100 / page 0 / page>100000 → 400).
    6. **Filtres** — exclusion archivées par défaut, `nonLuesUniquement`, `includeArchived` ; ordre `createdAt desc`.

- **5e domaine `notifications` — étape 9/9 (bascule flag gateway, volet code) FAIT** (2026-06-13) : `gateway.test.ts` étendu (19→22) — cas explicites domaine `notifications` (OFF défaut / canary allowlist / enabled+denylist ; extraction `notifications.markAsRead`/`notifications.generateOverdueReminders` ; parse env `NEW_STACK_DOMAINS`/`NEW_STACK_CANARY_NOTIFICATIONS`) + registre (notifications éligible). Volet code déjà en place (étape 5 : `notifications` dans `MIGRATED_DOMAINS`, gateway domaine-agnostique, garde-fou de montage `app.test` notifications.list→401). tsc src vert. **Suite src complète : 245/245 (45 fichiers)**. ⇒ **Gabarit notifications 9/9 (volet code) TERMINÉ — 5e domaine clean-archi livré bout-en-bout** (list paginée/filtrée + countUnread + markAsRead/markAllAsRead/archive anti-IDOR + generateOverdueReminders idempotent lecture seule factures). **OPE-257 → Done**. **Volet infra réel (déféré, groupé)** : bascule prod (proxy Cloudflare + flag `notifications` canary→cutover) avec vehicules+avis+badges+techniciens sous **OPE-216**.

- **6e domaine `fournisseurs` — étape 1/9 (scaffold + port) FAIT** (2026-06-13, **issue dédiée OPE-258** → In Progress) : `src/modules/fournisseurs/` — `domain/fournisseur.ts` (`Fournisseur` {nom, contact, email, telephone, adresse, codePostal, ville, notes}, Create/Update inputs), `application/fournisseur-repository.ts` (**port `IFournisseurRepository`** : list/getById/create/update/delete ; chaque méthode exige `TenantContext`), `fournisseurs.module.ts` (factory `createFournisseursModule`), `index.ts` (barrel sans infra). `fournisseurs` a un `artisanId` → RLS + filtre ; les **associations article↔fournisseur** (prix d'achat/références, table SANS artisanId, **IDOR historique OPE-90**) → ownership via fournisseur (étape 7). **Validé** (`fournisseurs.module.test.ts` 2/2). tsc src vert. Legacy `fournisseursRouter` (server/routers.ts L3601) : list/getById/create/update/delete + getArticleFournisseurs/associations (sous-ressource).

- **6e domaine `fournisseurs` — étape 2/9 (Repository Drizzle scopé tenant) FAIT** (2026-06-13) : `src/modules/fournisseurs/infra/fournisseur-repository-drizzle.ts` — `FournisseurRepositoryDrizzle` via `withTenant(ctx)` + filtre `artisanId` (table `fournisseurs`, RLS). `list` (order nom asc), `getById`, `create` (artisanId forcé), `update` (set + updatedAt, scopé → null), `delete` (ownership AVANT cascade → false sinon). **delete cascade** (transaction) : purge `articles_fournisseurs` (associations SANS artisanId : prix d'achat/références tenant-privés) avant le fournisseur — anti-IDOR. Mapper `toFournisseur`. **Validé PG (rôle app_tenant + RLS)** (`fournisseur-repository-drizzle.test.ts` 4/4) : CRUD scopés ; **isolation cross-tenant** (B ne lit/modifie/supprime pas le fournisseur de A, A intact) ; update champs scopés ; **delete cascade** (fournisseur + association purgés). tsc src vert. Suite fournisseurs 6/6.

- **6e domaine `fournisseurs` — étape 3/9 (use-cases de lecture) FAIT** (2026-06-13) : `src/modules/fournisseurs/application/read-use-cases.ts` — purs (repo injecté) `listFournisseurs`, `getFournisseur` (→ `NotFoundError` si null, ne révèle pas l'existence cross-tenant). + `infra/fournisseur-repository-fake.ts` (`FakeFournisseurRepository` in-memory scopé artisanId, miroir CRUD). **Validé** (`read-use-cases.test.ts` 4/4) : list scopé, getFournisseur tenant OK / autre tenant → NotFound (+ harnais) / inexistant → NotFound. tsc src vert. Suite fournisseurs 10/10.

- **6e domaine `fournisseurs` — étape 4/9 (use-cases d'écriture) FAIT** (2026-06-13) : `src/modules/fournisseurs/application/write-use-cases.ts` — purs async (repo injecté) `creerFournisseur` (valide `nom` non vide → `ValidationError`), `modifierFournisseur` (refuse `nom` vidé → Validation ; → `NotFoundError` si repo.update null), `supprimerFournisseur` (→ `NotFoundError` si repo.delete false). **Validé** (`write-use-cases.test.ts` 4/4) : create OK, nom vide → ValidationError, modifier OK / nom vidé → Validation / cross-tenant → NotFound (+ harnais), supprimer OK / cross-tenant → NotFound / déjà supprimé → NotFound. tsc src vert. Suite fournisseurs 14/14.

- **6e domaine `fournisseurs` — étape 5/9 (routeur tRPC + câblage + e2e) FAIT** (2026-06-13) : `src/modules/fournisseurs/interface/trpc/fournisseurs.router.ts` — `createFournisseursRouter(repo)`, transport mince. Procédures parité legacy : `list`, `getById`, `create`, `update` ({id,...} aplati → renvoie l'objet, cohérence inter-domaines), `delete` ({id}→{success}). **Bornes zod alignées table** (nom≤255, contact≤255, email email+≤320, telephone≤20, adresse≤500, codePostal≤10, ville≤100, notes≤5000). Domain errors → 404/400. **`createFournisseursModule` enrichi** (expose `.router`) + monté dans `createAppRouter` + `buildApp`/`AppDeps` (`fournisseurRepo?` injectable défaut Drizzle). **`fournisseurs` ajouté à `MIGRATED_DOMAINS`** (+ sampleProcedure app.test). **Validé e2e PG** (`fournisseurs.router.test.ts` 5/5) : 401 sans cookie, create+list scopés A, validation zod (nom vide → 400, email invalide → 400), **isolation cross-tenant** (B getById/update/delete fournisseur de A → 404, list vide, A intact), update+delete OK propriétaire. `fournisseurs.module.test` 3/3. tsc src vert. **Suite src complète : 266/266 (50 fichiers)**.

- **6e domaine `fournisseurs` — étape 6/9 (couverture e2e isolation exhaustive + bornes) FAIT** (2026-06-13) : `fournisseurs.router.test.ts` étendu (5→8 e2e) — getById/update/delete sur id inexistant du même tenant → 404 ; bornes zod (nom > 255 → 400, codePostal > 10 → 400, notes > 5000 → 400) ; **update partiel** ne touche pas les champs non fournis (ville modifiée, nom/contact préservés). tsc src vert. **Suite src complète : 269/269 (50 fichiers)**.

- **6e domaine `fournisseurs` — étape 7a/9 (associations article↔fournisseur : port + use-cases + Drizzle + fake) FAIT** (2026-06-13) : `domain/article-fournisseur.ts` (`ArticleFournisseur` {articleId, fournisseurId, referenceExterne, prixAchat string|null, delaiLivraison}, `AjouterAssociationInput`). Port étendu : `listAssociationsArticle`/`listAssociationsFournisseur` (→ [] si article/fournisseur hors tenant, **lecture sans oracle**), `ajouterAssociation` (→ null si article OU fournisseur hors tenant, **anti-IDOR 2 FK**), `supprimerAssociation` (→ false si l'assoc ne relève pas d'un fournisseur du tenant). **`articles_fournisseurs` SANS artisanId** → ownership via `articles_artisan.artisanId` (ownsArticle) ET `fournisseurs.artisanId` (ownsFournisseur), jointures scopées. Drizzle (withTenant + ownsArticle + ownsFournisseur + innerJoin scopé) + fake (`seedArticle`). Use-cases `association-use-cases.ts` : `listerFournisseursDeArticle`/`listerArticlesDeFournisseur` + `associerArticleFournisseur`/`dissocierArticleFournisseur` (→ NotFound si null/false). **Validé** (`association-use-cases.test.ts` 5/5) : associer+lister scopé, **anti-IDOR** (fournisseur d'un autre tenant → NotFound, article d'un autre tenant → NotFound, B ne lit pas le prix d'achat d'un article de A → [] sans oracle), dissocier OK/cross-tenant → NotFound/déjà supprimé. tsc src vert. **Suite src complète : 274/274 (51 fichiers)**. **IDOR historique OPE-90 fermé** (prix d'achat tenant-privé jamais exposé/modifié cross-tenant).

- **6e domaine `fournisseurs` — étape 7b/9 (routeur associations + e2e PG) FAIT** (2026-06-13) : procédures `getArticleFournisseurs`/`getFournisseurArticles`/`associateArticle`/`dissociateArticle` ajoutées à `createFournisseursRouter` (bornes zod : prixAchat regex décimal, referenceExterne≤100, delaiLivraison≥0). `fournisseurs.module.test` 3/3 (9 procédures). **Validé e2e PG** (`fournisseurs.router.test.ts` 9/9) : seed `articles_artisan` de A + fournisseur de A/B → associate (A) OK avec prixAchat ; getArticleFournisseurs (A) renvoie l'assoc + prix ; **anti-IDOR** (A associe avec le fournisseur de B → 404 ; B ne lit pas le prix d'achat de l'article de A → []) ; dissociate B → 404 / A → OK. Nettoyage articles_fournisseurs/articles_artisan. tsc src vert. **Suite src complète : 275/275 (51 fichiers)**. ⇒ associations article↔fournisseur complètes ; **IDOR OPE-90 fermé bout-en-bout** (transport→repo).

## Avancement global de la refonte (modèle transparent, recalculé à chaque firing)
Formule : `progress = 20% (Phase 0 socle : DB→PG + kernel TenantContext/RLS/withTenant + ports + gateway/flags + scaffold Fastify, FAIT) + 75% × (domaines_équivalents_terminés / 30) + 5% (bascule infra prod OPE-216 + extinction legacy, TODO)`. Base : ~50 routeurs legacy montés ≈ **~30 domaines métier significatifs** cibles. Domaines terminés bout-en-bout (9/9 volet code) : vehicules, avis, badges, techniciens, notifications = **5** ; + fournisseurs ≈ **0,9** (8e étape en cours). ⇒ `20% + 75%×(5,9/30) = 20% + 14,75% ≈ **~35%**`. (Estimation volontairement prudente ; le volet infra de bascule prod reste à 0.)

- **6e domaine `fournisseurs` — étape 8/9 (barrel complet + revue d'invariants) FAIT** (2026-06-13) : barrel `index.ts` complété (`domain/article-fournisseur` exporté ; toujours **sans impl Drizzle**) + `index.test.ts` (contrat public `createFournisseursModule`). Wiring déjà en place (étape 5). tsc src vert ; suite fournisseurs 30/30.
  - **Revue d'invariants du domaine fournisseurs (validée par tests)** :
    1. **Isolation cross-tenant** — `fournisseurs` : double cloisonnement RLS (`app_tenant` + GUC `app.tenant`) + filtre `artisanId`. Prouvé repo 4/4, read 4/4, write 4/4, assoc 5/5, e2e 9/9.
    2. **Anti-IDOR associations (OPE-90 fermé)** — `articles_fournisseurs` (SANS artisanId, **prix d'achat tenant-privé**) : ownership vérifié sur les **2 FK** (article via `articles_artisan.artisanId` + fournisseur via `fournisseurs.artisanId`). Lecture hors tenant → `[]` (**sans oracle**) ; écriture (associate/dissociate) hors tenant → **404**. Le prix d'achat d'un autre artisan n'est jamais lu ni modifié.
    3. **Cascade delete atomique** — suppression du fournisseur purge ses associations `articles_fournisseurs` dans la même transaction, après ownership.
    4. **Validations** — nom requis (Validation→400), bornes zod alignées table (nom≤255, email, adresse≤500, codePostal≤10, …), prix d'achat décimal (`^\d+(\.\d{1,2})?$`).

- **6e domaine `fournisseurs` — étape 9/9 (bascule flag gateway, volet code) FAIT** (2026-06-13) : `gateway.test.ts` étendu (22→25) — cas explicites domaine `fournisseurs` (OFF défaut / canary allowlist / enabled+denylist ; extraction `fournisseurs.associateArticle`/`fournisseurs.getArticleFournisseurs` ; parse env `NEW_STACK_DOMAINS`/`NEW_STACK_CANARY_FOURNISSEURS`) + registre (boucle sur les 6 domaines portés). Volet code déjà en place (étape 5). tsc src vert. **Suite src complète : 279/279 (52 fichiers)**. ⇒ **Gabarit fournisseurs 9/9 (volet code) TERMINÉ — 6e domaine clean-archi livré bout-en-bout** (CRUD + delete cascade + associations article↔fournisseur anti-IDOR prix d'achat OPE-90). **OPE-258 → Done**. **Volet infra réel (déféré, groupé)** : bascule prod avec les 5 autres domaines sous **OPE-216**.

## Avancement global de la refonte : **~36%** (6 domaines / ~30)
`20% (Phase 0 socle, FAIT) + 75% × (6/30) + 5% (bascule infra prod, TODO) = 20% + 15% = ~36%`. Domaines clean-archi terminés bout-en-bout (9/9 volet code) : **vehicules, avis, badges, techniciens, notifications, fournisseurs**. Reste ~24 domaines + la bascule prod (OPE-216 : déploiement Fastify staging + app_tenant/RLS + APP_DATABASE_URL + proxy Cloudflare).

- **7e domaine `commandes` (fournisseurs) — étape 1/9 (scaffold + port) FAIT** (2026-06-13, **issue dédiée OPE-259** → In Progress) : `src/modules/commandes/` — `domain/commande.ts` (`Commande`, `LigneCommande`, `CommandeStatut` brouillon|envoyee|confirmee|partiellement_livree|livree|annulee, `CommandeStatutFacturation` a_facturer|facturee, montants en string PG, `quantite`/`quantiteRecue`, Create/Update inputs avec `lignes`), `application/commande-repository.ts` (**port `ICommandeRepository`** : list/getById/listLignes/create/update/delete ; chaque méthode exige `TenantContext`), `commandes.module.ts` (factory), `index.ts` (barrel sans infra). `commandes_fournisseurs` a un `artisanId` → RLS+filtre ; `lignes_commandes_fournisseurs` (SANS artisanId) → scope via la commande. ⚠️ **Domaine sensible** : totaux calculés côté serveur (jamais client), quantité reçue ≤ commandée, fournisseur owned — invariants des étapes 4/7. **Validé** (`commandes.module.test.ts` 2/2). tsc src vert. Legacy `commandesFournisseursRouter` (server/routers.ts L3875) : list/getEnRetard/getById/create/update/updateStatut/setStatutFacturation/**recevoir**/delete/sendEmail/genererDepuisDevisIA.

- **7e domaine `commandes` — étape 2/9 (Repository Drizzle scopé tenant + totaux serveur) FAIT** (2026-06-13) : `src/modules/commandes/infra/commande-repository-drizzle.ts` — `CommandeRepositoryDrizzle` via `withTenant(ctx)` + filtre `artisanId`. `list` (dateCommande desc), `getById`, `listLignes` (ownership commande AVANT → [] sinon), `create` : **fournisseur owned** (→ null si fournisseur hors tenant, **anti-IDOR-FK**) → **totaux calculés serveur** (`calculerTotaux` : ligneHT = quantite×prixUnitaire, ligneTVA = ligneHT×tauxTVA/100, totaux = Σ, parité legacy) → insert commande (artisanId forcé, statut brouillon, numero `CMD-NNNNN` dérivé de l'id) + lignes (montantTotal = ligneHT) en transaction. `update`/`delete` scopés (delete = cascade lignes après ownership). **Validé PG (rôle app_tenant + RLS)** (`commande-repository-drizzle.test.ts` 4/4) : create totaux corrects (**invariant Σ lignes.montantTotal = totalHT** ; TVA multitaux 20%+10% exacte ; TTC = HT+TVA), **anti-IDOR-FK** (fournisseur de B → null), **isolation cross-tenant** (B ne lit/modifie/supprime pas la commande de A, listLignes → []), delete cascade lignes. tsc src vert. Suite commandes 6/6.

- **7e domaine `commandes` — étape 3/9 (use-cases de lecture) FAIT** (2026-06-13) : `src/modules/commandes/application/read-use-cases.ts` — purs (repo injecté) `listCommandes`, `getCommande` (→ `NotFoundError` si null), `listLignesCommande` (→ [] si commande hors tenant, lecture sans oracle). + `infra/commande-repository-fake.ts` (`FakeCommandeRepository` in-memory scopé artisanId + `seedFournisseur` ; reproduit calcul totaux serveur + lignes scopées + anti-IDOR-FK create → null). **Validé** (`read-use-cases.test.ts` 5/5) : list scopé, getCommande tenant OK (totaux serveur HT 10 / TTC 12) / autre tenant → NotFound (+ harnais) / inexistant → NotFound, listLignes scopé + commande hors tenant → []. tsc src vert. Suite commandes 11/11.

- **7e domaine `commandes` — étape 4/9 (use-cases d'écriture) FAIT** (2026-06-13) : `src/modules/commandes/application/write-use-cases.ts` — purs async (repo injecté) `creerCommande` (valide ≥1 ligne, ≤500, chaque ligne designation non vide + `quantite>0` + prix≥0 → `ValidationError` ; → `NotFoundError` si repo.create null = **fournisseur hors tenant, anti-IDOR-FK**), `modifierCommande` (**métadonnées seulement** : reference/notes/adresse/dateLivraison ; → `NotFoundError` si null), `supprimerCommande` (→ `NotFoundError` si false). **Invariant préservé** : totaux calculés serveur, jamais via update. **Validé** (`write-use-cases.test.ts` 6/6) : create OK (totaux serveur) / sans ligne → Validation / quantité≤0 → Validation / désignation vide → Validation, fournisseur d'un autre tenant → NotFound, modifier OK / cross-tenant → NotFound (+ harnais), **modifier ne touche pas les totaux** (HT/TTC inchangés), supprimer OK / cross-tenant → NotFound / déjà supprimée → NotFound. tsc src vert. Suite commandes 17/17.

## Avancement global de la refonte : **~38%** (6 domaines terminés + commandes 4/9)
`20% (Phase 0 socle FAIT) + 75% × (6,4/30) + 5% (bascule infra prod TODO) ≈ ~38%`.

## Prochaine action : **commandes — étape 5/9 : routeur tRPC + câblage + e2e**
`src/modules/commandes/interface/trpc/commandes.router.ts` : `createCommandesRouter(repo)` (transport mince). Procédures (parité legacy `commandesFournisseursRouter`, périmètre CRUD ce firing) : `list` (→ `listCommandes`), `getById` (→ `getCommande` ; éventuellement renvoyer commande + lignes via un appel composite ou laisser le front appeler `getLignes`), `getLignes` ({commandeId} → `listLignesCommande`), `create` (input {fournisseurId, reference?, dateLivraisonPrevue?, adresseLivraison?, notes?, lignes:[{articleId?, designation, reference?, quantite:number, unite?, prixUnitaire?:number, tauxTVA?:number}] max 500} → `creerCommande` ; **mapper number→string pour le repo**), `update` ({id, reference?/notes?/adresseLivraison?/dateLivraisonPrevue?} → `modifierCommande`), `delete` ({id} → `supprimerCommande`→{success}). **Bornes zod** alignées (reference≤50, designation≤255, lignes max 500, quantite>0, tauxTVA 0..100). Domain errors → 404/400. Câbler `createCommandesModule` enrichi (exposer `.router`) + monter dans `createAppRouter`/`buildApp`/`AppDeps` (`commandeRepo?` injectable défaut Drizzle) + ajouter `commandes` à `MIGRATED_DOMAINS` (+ sampleProcedure `commandes.list`). ⚠️ NB : le legacy monte ce routeur sous la clé **`commandesFournisseurs`** ; choisir le préfixe du nouveau stack (`commandes` recommandé, cohérent avec le module) et le documenter. Gate `tsc src` + **e2e PG** : 401 sans cookie, create (fournisseur seedé) + list scopés, isolation cross-tenant (getById/update/delete d'une commande d'un autre tenant → 404 ; create avec fournisseur de B → 404), validation (sans ligne → 400). Pas de `OPE-XXX` en code. Puis 4 canaux (Linear OPE-259 ; ntfy avec % refonte) → 6/9 e2e exhaustif, 7/9 logique dérivée (updateStatut/**recevoir** réception≤quantité→stock/getEnRetard/setStatutFacturation), 8-9. Gabarit identique aux 6 domaines précédents.

### (archive) Spécif détaillée 7 (origine) : **sous-ressource associations article↔fournisseur (anti-IDOR prix d'achat)**
Brancher les associations dans le routeur : ajouter à `createFournisseursRouter` les procédures parité legacy : `getArticleFournisseurs` ({articleId} → `listerFournisseursDeArticle`), `getFournisseurArticles` ({fournisseurId} → `listerArticlesDeFournisseur`), `associateArticle` ({articleId, fournisseurId, referenceExterne?, prixAchat?, delaiLivraison?} → `associerArticleFournisseur`), `dissociateArticle` ({id} → `dissocierArticleFournisseur` puis `{success:true}`). Bornes zod (prixAchat regex décimal, referenceExterne≤100). Mettre à jour `fournisseurs.module.test` (9 procédures). **e2e PG** (`fournisseurs.router.test.ts`) : seed via admin un article (`articles_artisan` de A) + un fournisseur de A + un fournisseur/article de B → `associateArticle` (A) OK ; `getArticleFournisseurs` (A) renvoie l'assoc ; **anti-IDOR** : A associe avec le fournisseur de B → 404, B ne lit pas le prix d'achat de l'article de A → [] ; `dissociateArticle` OK / cross-tenant → 404. Nettoyage articles_fournisseurs/articles_artisan dans before/afterAll. ⚠️ vérifier colonnes `articles_artisan` requises non-null (reference, designation, prixUnitaireHT). Gate `tsc src` + tests. Pas de `OPE-XXX` en code. Puis 4 canaux (Linear OPE-258) → 8/9 wiring+invariants, 9/9 bascule flag.

### (archive) Spécif détaillée 7 (origine) : **sous-ressource associations article↔fournisseur (anti-IDOR prix d'achat)**
Porter les associations article↔fournisseur (parité legacy `fournisseursRouter.getArticleFournisseurs` + add/delete association). **Table `articles_fournisseurs` SANS artisanId** (prix d'achat/références = données tenant-privées, **IDOR historique OPE-90**) → scope OBLIGATOIRE via ownership : l'**article** appartient au tenant (`articles_artisan` a un `artisanId` → RLS+filtre) ET le **fournisseur** appartient au tenant (`fournisseurs.artisanId`). Domain `ArticleFournisseur` {id, articleId, fournisseurId, referenceExterne, prixAchat (string|null), delaiLivraison (number|null), createdAt}. Étendre `IFournisseurRepository` : `listAssociationsArticle(ctx, articleId)` (→ [] si article hors tenant), `ajouterAssociation(ctx, {articleId, fournisseurId, ...})` (→ null si article OU fournisseur hors tenant), `supprimerAssociation(ctx, id)` (→ false si l'association ne relève pas d'un article/fournisseur du tenant). Drizzle (withTenant + ownsArticle + ownsFournisseur) + fake ; use-cases + routeur (`getArticleFournisseurs`/`ajouterArticleFournisseur`/`supprimerArticleFournisseur`) ; tests unit + e2e PG : isolation cross-tenant (B ne lit/écrit pas le prix d'achat d'un article/fournisseur de A → []/404). ⚠️ vérifier le legacy pour le nom/forme exacts des procédures associations (server/routers.ts ~L3686). Gate `tsc src` + tests. Pas de `OPE-XXX` en code. Puis 4 canaux (Linear OPE-258) → 8/9 wiring+invariants, 9/9 bascule flag (cas `fournisseurs` au gateway.test). Gabarit identique (sous-ressource type techniciens positions/disponibilités).

### (archive) Spécif détaillée 7 (origine) : **logique dérivée (generateOverdueReminders)**
Porter `generateOverdueReminders` (parité legacy `notificationsRouter`) : créer des **notifications de rappel** pour les **factures impayées en retard** du tenant. **D'abord inspecter** le legacy (server/routers.ts ~L2581) + `db.getFacturesByArtisanId` : la logique = factures où `statut != payee && statut != annulee && dateEcheance < now` → pour chacune, créer une notification type `rappel` (titre/message avec client + jours de retard + lien). **Modéliser** : port `INotificationRepository.creerRappelsFacturesEnRetard(ctx)` OU un port lecture `IFactureEnRetardRepository.listEnRetard(ctx)` + use-case orchestrant la création via un `creer(ctx, input)` ajouté au port notifications (insert notification scopé tenant). **Idempotence** : éviter les doublons de rappel (le legacy ne déduplique pas explicitement — vérifier ; à minima ne pas recréer si un rappel non lu existe déjà pour la même facture/échéance, OU documenter le comportement legacy reproduit). **Tout scopé tenant** (factures.artisanId = ctx.artisanId + RLS). **Découpe** : si volumineux, porter d'abord le **port lecture factures en retard + use-case + fake + tests unitaires** (1 firing), puis Drizzle + routeur `generateOverdueReminders` + e2e (firing suivant). ⚠️ Domaine sensible (facturation) : ne PAS altérer les factures, lecture seule ; préserver l'isolation cross-tenant. Gate `tsc src` + tests. Pas de `OPE-XXX` en code. Puis 4 canaux (Linear OPE-257) → 8/9 wiring+invariants, 9/9 bascule flag (cas `notifications` au gateway.test ; déjà au registre). Gabarit identique aux domaines précédents.

### (archive) Spécif détaillée 7b (origine) : **logique dérivée ÉCRITURE (calculerClassement + verifierBadges)**

### (archive) Spécif détaillée 7b (origine) : **logique dérivée ÉCRITURE (calculerClassement + verifierBadges)**
Porter les **recalculs/auto-attributions** (parité legacy `db.calculerClassement` L5670 + `db.verifierEtAttribuerBadges` L5612). **`calculerClassement(ctx, periode)`** : agrège par technicien les **interventions `terminee`** de la période (bornes : semaine = -7j, mois = 1er du mois, trimestre = 1er du trimestre, annee = 1er janvier) **LEFT JOIN factures payées** (CA = SUM totalTTC, condition `statut='payee'` dans le ON), `points = interventions*10 + floor(ca/100)`, **purge** du couple (artisanId,periode,dateDebut) puis **insert** des lignes rangées (rang par ordre interventions desc, ca desc), renvoie le classement. **Tout scopé tenant** (`interventions.artisanId = ctx.artisanId`, RLS sur interventions/factures/classement_techniciens). **`verifierBadges(ctx, technicienId)`** : nécessite d'abord d'inspecter `db.verifierEtAttribuerBadges` (seuils par catégorie → attribution auto via `attribuer`, déjà idempotent+anti-IDOR). **Découper** : porter `calculerClassement` d'abord (port write `recalculerClassement` + Drizzle transactionnel purge+insert + use-case + routeur `calculerClassement` mutation + e2e PG : seed interventions/factures, vérifier rangs/points scopés tenant, isolation), puis `verifierBadges` au firing suivant si volumineux. ⚠️ Vérifier les colonnes `factures` (totalTTC, statut, id) et `interventions` (statut enum `terminee`, dateDebut, technicienId, factureId, artisanId) avant de coder. Gate `tsc src` + tests. Pas de `OPE-XXX` en code. Puis 4 canaux (Linear OPE-217) → 8/9 wiring module complet + revue invariants, 9/9 bascule flag (déjà au registre ; infra groupée OPE-216).

### (archive) Spécif détaillée 7 (origine) : **workflow email demande d'avis (envoyerDemande / envoyerDemandeParClient)**
Porter la logique métier d'envoi de demande d'avis (parité legacy `avisRouter.envoyerDemande` {interventionId} et `envoyerDemandeParClient` {clientId}). Nouveau port lecture/écriture : `IDemandeAvisRepository` (table `demandes_avis`) — `creerDemande(ctx, {clientId, interventionId, tokenDemande, emailEnvoyeAt, expiresAt})` scopé tenant ; + accès ownership `intervention`/`client` (réutiliser un port `clients`/`interventions` minimal **scopé tenant** : `getInterventionById(ctx,id)`, `getClientById(ctx,id)`, `getDerniereInterventionDuClient(ctx,clientId)` → NOT_FOUND uniforme cross-tenant, **anti-oracle d'énumération**). Use-cases `envoyerDemandeAvis(repo, emailPort, ctx, interventionId)` et `envoyerDemandeAvisParClient(...)` : valider ownership (NotFound), client.email requis (ValidationError), générer token (uuid) + expiresAt (+14j), créer la demande, envoyer l'email via **EmailPort** (lien `${APP_URL}/avis/${token}`, source de confiance APP_URL pas Origin), **rate limit** par artisan (port RateLimiter ou clé dédiée `avis:<artisanId>`). Router : `envoyerDemande`/`envoyerDemandeParClient` (mutations). Gate `tsc src` + tests use-cases avec fakes (EmailPort fake = capture, repo fake) : succès envoi (1 email capté, demande créée), ownership cross-tenant → NotFound, sans email → Validation, rate limit dépassé → erreur. Pas de `OPE-XXX` en code. Puis 4 canaux → 8/9 wiring module complet + revue invariants, 9/9 bascule flag (gateway). Gabarit identique à vehicules.
La **bascule vehicules (9/9, OPE-216)** est un volet **infra/déploiement** (déployer le stack Fastify sur staging + `app_tenant`/RLS staging + `APP_DATABASE_URL` + proxy Cloudflare `functions/api/[[path]].js` + flag canary). Plutôt que de le faire isolément, **démarrer le 2e domaine** pour accélérer la couverture des phases 1→5, puis grouper la bascule quand plusieurs domaines sont prêts. **Prochaine sous-tâche concrète** : choisir un domaine Phase 1 simple (ex. `support` OPE-218, ou `badges/techniciens`) et faire l'**étape 1/9 (scaffold module + port)** en suivant le gabarit vehicules (cf. recette). D'abord inspecter le schéma PG du domaine (tables + colonnes + RLS) puis créer `src/modules/<domaine>/{domain,application,vehicules.module-like}`. Gate `tsc src` + test stub. Pas de `OPE-XXX` en code. Puis 4 canaux. (Garder OPE-216 bascule pour un créneau infra groupé.)
Restent : 7/9 (OPE-214) et 8/9 (OPE-215) — récupérer les titres exacts (probable : tests d'intégration complémentaires / wiring module / observabilité) et livrer ; 9/9 **bascule flag** (OPE-216) = câbler le proxy Cloudflare `functions/api/[[path]].js` consommant `shouldRouteToNewStack` + flag `vehicules` (canary→cutover). ⚠️ La bascule réelle nécessite : déployer le nouveau stack **Fastify sur staging** (service + port + command), créer `app_tenant` + appliquer `tenant-isolation.sql` **sur staging PG**, définir `APP_DATABASE_URL` (rôle app) dans l'env du nouveau stack. **Prochaine sous-tâche concrète** : lire OPE-214/215 (`get_issue`) et exécuter la suivante du gabarit ; si purement « tests/wiring » déjà couverts, avancer (marquer Done) vers la bascule OPE-216 (volet infra) OU démarrer le **2e domaine** (gabarit répété : badges/techniciens ou support) pour accélérer la couverture des phases 1-5 pendant que la bascule infra attend un créneau de déploiement. Restent les étapes du gabarit : 6/9 couverture e2e isolation exhaustive (appliquer le harnais sur toutes les routes by-id du routeur — getById/update/delete/updateKilometrage/addEntretien/addAssurance en cross-tenant → NOT_FOUND), 7/9 tests entretiens/assurances e2e, 8/9 perfs/observabilité (optionnel à ce stade), 9/9 **bascule flag** (OPE-216 : câbler le proxy Cloudflare `functions/api/[[path]].js` + flag `vehicules` → canary → cutover ; nécessite aussi le déploiement du nouveau stack Fastify sur staging + `APP_DATABASE_URL` + RLS staging — cf. R0.12b reste). **Prochaine sous-tâche concrète : 6/9** — `vehicules.router.isolation.test.ts` : harnais `expectCrossTenantDenied` (adapté aux codes HTTP 404/403) sur TOUTES les routes by-id du routeur, en e2e. Gate `tsc src` + tests. Puis 4 canaux. (Ensuite, envisager d'industrialiser le gabarit en générateur pour les domaines suivants des phases 1→5.)
