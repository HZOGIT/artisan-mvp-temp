// Composant de statut partagé du front neuf — copie conforme legacy (ré-export). Présentation
// (icône + couleur + libellé par statut) réutilisée par devis/factures/interventions. Cf. button.ts.
export * from "@/components/StatutBadge";
