// Primitive UI du front neuf — COPIE CONFORME du composant legacy (ré-export : composant identique →
// parité pixel garantie, aucune divergence possible). Le front neuf importe depuis `modern/shared/ui`
// (surface stable, découplée du chemin legacy `@/shared/ui-kit`). À la suppression finale du legacy,
// ces primitives shadcn seront physiquement relocalisées ici (déplacement mécanique, sans changement).
export * from "@/shared/ui-kit/button";
